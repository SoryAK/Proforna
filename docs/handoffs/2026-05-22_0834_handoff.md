# Session Handoff — 2026-05-22 08:34

## Current Sprint

Worklog notes three-pane redesign (ADR 0007) with Tiptap 3 + Y.js editor (ADR 0010). This session focused on fixing a critical regression in the editor seed path that was silently wiping previously-saved entries on open.

## Last Completed Step

Fixed the "editor opens empty and overwrites the saved entry" bug by restructuring the editor mount so the Y.Doc is seeded from the server snapshot **before** the Tiptap Collaboration plugin binds. Detected effectively-empty fragments (stale empty-paragraph state left by prior buggy mounts) via text-stripped XML check, wipe in-memory + IndexedDB cache (fire-and-forget), then apply seed. Also reduced loading flash and shortened the sync fallback.

- Commit: `3700c09` on `feature/interactive-resume-split-layout` — 93 files (full worklog three-pane snapshot, untracked → committed).
- Files touched this session: [src/components/worklog/worklog-editor.tsx](src/components/worklog/worklog-editor.tsx)
- Observable outcome: opening a saved worklog entry → clicking Edit now shows the existing content immediately. Typing extends (not replaces). Autosave preserves the seeded body. Load feels near-instant.

## The "Live" Context

- **Architecture**: outer `WorklogEditor` awaits `yHandle.synced` → seeds Y.Doc via `prosemirrorJSONToYDoc(seedSchema, json, "default")` + `Y.applyUpdate` → sets `ready=true` → `EditorBody` mounts. Inner editor never sees an empty doc.
- **Module-level seed schema**: `seedSchema = getSchema(SCHEMA_EXTENSIONS)` built without instantiating an editor — required because seeding happens before mount.
- **Effective-emptiness check**: `fragment.toString().replace(/<[^>]*>/g, "").trim().length === 0` — catches the `<paragraph></paragraph>` stale state that pure `fragment.length === 0` missed.
- **Loading UX**: invisible 6rem-height placeholder + 400ms fallback (was 2s).
- **Multi-device caveat still parked** in `/memories/repo/parked-ideas.md` — `TODO(multi-device)` marker in code points there. Server is last-write-wins until a Yjs network provider (y-websocket / Hocuspocus / Liveblocks) is added.
- **OneDrive git locks** confirmed again: commit triggered "Should I try again? (y/n)" prompts for `.git/worktrees/*/logs`; answering `n` lets commit complete safely.

## Next Immediate Step

> "Resume the worklog three-pane sprint. Editor seed bug is fixed and committed (3700c09). Pick the next item from the three-pane redesign backlog — likely either (a) wiring the slash command menu polish, (b) photo-node-view refinements, or (c) the worklog-folders-rail interactions. Open docs/adr/0007 and the IMPROVEMENTS.md to decide priority."

## Unresolved Blockers

- [ ] Multi-device simultaneous edits still last-write-wins on server (parked, not a blocker for solo use).
- [ ] Prisma `predev` occasionally hits `EPERM` renaming `query_engine-windows.dll.node` because of OneDrive — pause OneDrive sync before `npm run dev` if it recurs.
- [ ] Two ghost worktrees can't be pruned by git due to OneDrive locks: `agents-capabilities-overview`, `agents-review-master-gallery-feature`. Cosmetic; doesn't affect commits.
- [ ] `npm run dev` exit code 1 in last terminal — caused by the Prisma EPERM above, not by code changes.

## Optional: Deferred Ideas

- **Yjs network provider for multi-device** (logged in `/memories/repo/parked-ideas.md`): add y-websocket or Hocuspocus to convert "local-CRDT + debounced-snapshot" into true multi-device collaboration.
- **Three-pane redesign sprint items** untouched this session: see ADRs 0007–0010 and `IMPROVEMENTS.md`.

## CodeGraph Friction

None this session — the bug was isolated to a single file and a known architectural pattern (Tiptap 3 + Y.js seed timing). No graph queries were needed; direct reads and edits sufficed.
