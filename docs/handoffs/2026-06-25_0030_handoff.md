# Handoff — 2026-06-25 00:30

## Current Sprint

**Battle-tested review pipeline — Tier B selective polish.** This session continued from the 2026-06-24 23:30 handoff. Tier A (auto-enforce existing rigor on push) + ETRC (auto-classify and selectively design-review on commit) were both live entering this session. The work this turn was a tightly-scoped Tier B sprint — three items off `/memories/repo/parked-ideas.md` (B5 + B1 + B3 thin), shipped as three separate ETRC commits per the re-entry discipline that forbids bundling. Tier C explicitly NOT picked up — all five items are structurally gated on triggers that have not fired (second contributor, paid audit), per parked-ideas.md line 72.

## Last Completed Step

`0b98016` — `feat(review): --since shorthand (Ndays/Nweeks/SHA) in review:queue (Tier B3 thin)`.

Followed this turn by Phase 4 close-out: `mcp_memory-resums_open_nodes(["ReviewPipeline", ...])` ran (success — yesterday's 503 has cleared); MCP graph appended with 4 new 2026-06-24 observations on `ReviewPipeline` covering the Tier B trio + Tier C-non-action; shard `docs/memory/review-pipeline.json` updated to match; Practice Adoption probe asked verbatim — user replied **none**; `workflow-change-log.json` NOT appended this session.

## The "Live" Context

- **3 commits this session**, all on the Tier B sprint. ZERO pushes (35 commits from the morning push had cleared the backlog; today's three sit unpushed by user policy).
- **`66887ea`** — Tier B5. Semgrep `--baseline-commit` mode wired into `scripts/review-semgrep.mjs`. Env var `RESUMSIFY_REVIEW_BASELINE_COMMIT` (set by `scripts/review-phase.mjs`) injects `--baseline-commit <sha>` into the semgrep call; only NEW findings since baseline fail the gate. `semgrep/rules/no-window-prompt.yml` promoted WARNING → ERROR — the bug class is now architecturally locked out for new code while the 56 historic callsites drain organically per the parked-ideas opportunistic-cleanup discipline.
- **`7ab910a`** — Tier B1. Per-folder coverage gate. `@vitest/coverage-v8@4.1.9` installed. `vitest.config.ts` gained `coverage: { provider: 'v8', reporter: ['text', 'json-summary'], reportsDirectory: 'coverage' }`. `scripts/review-phase.mjs` now reads `coverage/coverage-summary.json` after the vitest pass and enforces per-folder floors from `docs/review-baseline.json` `coverage` field. Hard-fail (no `--accept-net` offset path). Initial floors at baseline commit: `src/lib=48.06%` / `src/app/api=10.59%` / `src/data=0%`.
- **`0b98016`** — Tier B3 thin. `npm run review:queue --since <X>` extended from ISO-only to three shapes via `resolveSinceTs()` in `scripts/review-queue-read.mjs`:
  - Relative duration: `/^(\d+)\s*(d|days?|w|weeks?)$/i` → `7days`, `30d`, `2weeks`, `2w`
  - Git SHA: `/^[0-9a-f]{7,40}$/i` resolved via `spawnSync('git', ['log', '-1', '--format=%cI', sha])` → cutoff = that commit's committer date
  - ISO / `Date.parse()` fallback (back-compat)
  - Graceful failure: malformed input emits stderr warning + skips the filter (does NOT silently include everything as if `--since` were absent)
  - Resolved label printed in header: `Filter: since=7days (cutoff: 2026-06-17T23:16:42.222Z)`
- `docs/workflows/etrc-rhythm.md` review-day cadence section reframed around `--since 7days` weekly window on all three tier reads (added the `--` separator npm requires); prompt-tuning section flipped from `YYYY-MM-DD` to `--since 30days`.
- `docs/review-baseline.json` regenerated at `66887ea` after B5 ship: tsc=29, eslint=519, knip.deps=1, madge=0, vitest 1017/5/0/1022, coverage src/lib=48.06% / src/app/api=10.59% / src/data=0%, total=549. This is the contract going forward.
- **Tier B parked-ideas re-entry discipline honored**:
  - B4 (window-prompt cleanup sprint) — NOT opened, anti-recommendation respected; baseline-commit gate from B5 + WARNING already prevent new occurrences.
  - B2 (commit-msg trailer enforcement) — trigger not fired (no missed-trailer incidents).
  - One item per commit — bundling explicitly refused even when user said "all of Tier B."
- **Tier C — structurally not-now**: CodeRabbit/Greptile (C1), GitHub Actions CI for review:phase (C2), branch protection (C3), SonarQube/CodeQL/Semgrep Pro (C4), Reviewable (C5) — all gated on second-contributor or paid-audit triggers that have not fired. The Architectural Reviewer call surfaced the structural-not-friction distinction; user agreed to skip and wrap.
- MCP memory-resumsify is **back to healthy** (yesterday's 503 cleared since the VS Code MCP restart took effect on session start). The graph received today's 4 new observations directly; the shard is in parity.

## Next Immediate Step

**Push the day's work, then take the new review:phase gate for a real spin.**

1. `git -c gc.auto=0 push` — three commits land on remote (`66887ea`, `7ab910a`, `0b98016`). Dependabot has already been activated by yesterday's 35-commit push, so this push is incremental.
2. Resume normal ETC cycles for any next feature work. The post-commit hook auto-classifies; the per-folder coverage gate now catches any test-deletion that hides assertions; the pre-push gate enforces no regression on tsc/eslint/knip/madge/vitest/coverage/Semgrep all together.

**Optional pickups (parked, do NOT pull without trigger):**

- **R-local v1 prompt tuning** — still parked under ADR-0049 promotion candidates after 5–10 risky commits accumulate. Two empty-response data points so far. Not enough signal.
- **Tier B4 opportunistic cleanup** — chip at the 56 prompt/confirm/alert callsites only when touching those files for OTHER reasons. Do NOT open a dedicated sprint.
- **Column 3 longitudinal verification + commit queue** — re-entry trigger "≥5 commits reviewed AND ≥2 material findings" not fired (currently 2/1).

## Unresolved Blockers

1. **Three commits unpushed by policy** — `66887ea` + `7ab910a` + `0b98016`. Push when user authorizes.
2. **R-local v1 prompt returns `skipped:empty-response` on small risky diffs** — carried from yesterday's handoff, still 2-for-2. Contract holds (logged, joinable, escalate-able). Tuning parked.
3. **Tier B leftovers not promoted**: B2 (commit-msg trailer enforcement) and B4 (window-prompt cleanup) both intentionally deferred — triggers not fired. No action required.

## UI Graph Status

UI Graph Keeper did NOT run this session — no `.tsx` files modified, no UI surface changes. All work was dev-infrastructure (`scripts/`, `vitest.config.ts`, `package.json`, `semgrep/rules/`, `docs/workflows/`, `docs/review-baseline.json`, JSON memory shard). No `.github/ui/` audit was warranted.

## Memory Graph Status

Memory Keeper ran `mcp_memory-resums_open_nodes(["ReviewPipeline", "SemgrepRules", "LefthookHooks", "ETRCRhythm", "ReviewPhase"])` — only `ReviewPipeline` exists (the other names returned empty, confirming the shallow-graph rule). Appended 4 new 2026-06-24 observations to `ReviewPipeline` via `mcp_memory-resums_add_observations`:

- Tier B5 ship details (Semgrep `--baseline-commit` env-wired; window-prompt rule promoted to ERROR).
- Tier B1 ship details (`@vitest/coverage-v8` install, per-folder floors in baseline, hard-fail policy).
- Tier B3 thin ship details (`resolveSinceTs()` three-case dispatch, graceful failure, resolved-label print).
- Tier B/C re-entry discipline honored (B4/B2 deferred per anti-recommendation/trigger-not-fired; Tier C entirely declined per structural gating).

Shard `docs/memory/review-pipeline.json` updated with the same 4 observations for durability (rebuild protocol).

## Workflow Review Cadence

✅ on cadence — 5 days since last review (target: weekly). This session did NOT perform a workflow review (it shipped a Tier B feature sprint, not rule-edits to `.github/instructions/`); `docs/chat-exports/analysis/workflow-reviews.json` was NOT appended.

## User Practice Adoptions

Probed verbatim per the Phase 4 composite gate. User replied **none**. No new adoptions tracked this session — `workflow-change-log.json` NOT appended. The Tier B work is captured under existing review-pipeline tooling history; the strict-one-item-per-commit discipline and Architectural-Reviewer-refuses-Tier-C reflex are surfaced patterns of existing rules (parked-ideas.md line 73, structural.instructions.md Architectural Guardrail), not new practices.

## Session Commit Trail (this session)

1. `66887ea` — `feat(review): Semgrep --baseline-commit mode + promote no-window-prompt to ERROR` (Tier B5)
2. `7ab910a` — `feat(review): per-folder coverage gate in review:phase (Tier B1)`
3. `0b98016` — `feat(review): --since shorthand (Ndays/Nweeks/SHA) in review:queue (Tier B3 thin)`

All three on `main`, none pushed. `git status` clean post-commit.

## Pre-resume checklist for next session

1. **Read the most recent handoff** (this file).
2. **Verify push state** — `git log --oneline origin/main..HEAD` to confirm whether the 3 Tier B commits have been pushed since.
3. **Read workflow-reviews.json cadence line** at session start — 5 days at handoff time, ticks toward "due" at 7 days.
4. **MCP health check** — `mcp_memory-resums_open_nodes(["ReviewPipeline"])` should succeed immediately; if 503 returns, Command Palette → MCP: Restart Server → memory-resumsify per user-memory note 2026-06-15.
