# Session Handoff — 2026-06-21 21:14 (local)

## 1. Current Sprint

**ADR-0044 — AI Provider Router (Scope B):** week-long sprint to consolidate the 14 fragmented Gemini callers behind a unified `ai.generate()` façade, add a Gemini 2.5 Pro premium tier for the `reason` task class, and wire end-to-end `onUsage` token-accounting hooks. **Closed today.**

## 2. Last Completed Step

Day 5 wrap shipped in three commits:

- **`e5fcbe0` — fix(ai):** replace decommissioned `gemini-1.5-flash` / `gemini-1.5-pro` fallback models. Fast chain → `[primary, gemini-2.0-flash-lite, gemini-2.0-flash]`. Pro chain → `[primary]` only (no in-tier fallback by design — degrading a reasoning task to a fast model is worse than surfacing the quota error). One test renamed + assertion updated.
- **`1472f75` — test(ai):** Day 5 smoke suites. `src/lib/ai/smoke.test.ts` (6 hermetic cases, always runs). `src/lib/ai/smoke.live.test.ts` (5 cases, env-gated by `RUN_LIVE_SMOKE_AI=1`, accepts happy-path OR 503/429 as green). `scripts/smoke-ai-live.ps1` runner with `.env` quote-stripping. `scripts/probe-gemini.ps1` diagnostic.
- **`18c0fbb` — docs(adr):** ADR-0044 graduated from Proposed to Accepted. Added Acceptance subsection enumerating the 5 sprint commits + lab evidence.

**Lab evidence at acceptance:**

- Hermetic: **860/860 green** (Vitest 4.1.9, Node env, 854 prior baseline + 6 new smoke)
- Live: **5/5 green** (1 real happy-path: `summarize` via ollama `gemma4:26b` returned text + token counts `{p:51, c:534}`; 4 accepted error-path 429s from Gemini free-tier quota exhaustion)

## 3. The "Live" Context

- **`src/lib/ai/providers/gemini-fast.ts:259-263`** — fast-tier `modelChain` call site. Default chain now `[primary, gemini-2.0-flash-lite, gemini-2.0-flash]`. If `process.env.GEMINI_MODEL` is unset, primary defaults to `gemini-2.0-flash-lite` and modelChain dedupes to a 2-model chain.
- **`src/lib/ai/providers/gemini-pro.ts:67-68`** — pro-tier chain. Default `[primary]` only. `modelOverride` parameter still bypasses if set.
- **`src/lib/ai/smoke.test.ts`** — hermetic; uses `vi.stubGlobal("fetch", ...)` per test to stub Ollama + Gemini responses; exercises router selection logic, fallback paths, and `onUsage` callback firing.
- **`src/lib/ai/smoke.live.test.ts`** — env-gated via `describe.skipIf(!RUN_LIVE)`. `expectGreenOrAcceptableError(label, fn)` helper accepts `AIProviderError` with `status === 503 || 429`. Per-test `LIVE_TIMEOUT = 60_000ms` for gemma4:26b cold start.
- **`scripts/smoke-ai-live.ps1`** — naïve `.env` split-on-`=` would pass surrounding quote chars through to Gemini → 400 bad-key. The included `Read-EnvValue`-style loop strips surrounding `"` or `'` pairs. Forces `OLLAMA_MODEL=gemma4:26b` (the actually-installed model — `llama3.2` is NOT installed locally; default config would silently 404 on `ollama_chat`).
- **`docs/adr/0044-ai-provider-router.md`** — line 3 now `Accepted`; new Acceptance section at file tail enumerates commits + parked follow-ups.

## 4. Next Immediate Step

ADR-0044 sprint is fully closed. Suggested next-session opening prompt:

> *"Pick the next item from `/memories/repo/parked-ideas.md` or the active feature backlog. Two parked follow-ups from ADR-0044 are worth considering: (a) `geminiErrorMessage()` swallowing diagnostic bodies for non-429 errors; (b) the unsynced prior handoff `docs/handoffs/2026-06-20_0016_handoff.md` still untracked in this worktree — read it and decide if anything in it is still in-flight."*

## 5. Unresolved Blockers

- **`geminiErrorMessage()` blandness (parked, not blocking):** for non-429 Gemini responses, returns generic `"AI extraction failed"` and swallows the underlying error body. Diagnostics suffer when the real cause is something other than quota. Captured in `/memories/repo/parked-ideas.md` and `AIServicesLayer` memory entity.
- **Free-tier Gemini quota = 0 on dev account:** no live happy-path against Gemini is possible from this lab. All ADR-0044 live-smoke runs against Gemini will accept 429 as expected error-path. Production deployments must either enable Gemini billing OR rely entirely on the `ollama` tier. Captured in user memory.
- **Prior session handoff `docs/handoffs/2026-06-20_0016_handoff.md` still untracked.** Not opened or read this session — belongs to a separate prior session. Worth scanning at next session start.

## 6. UI Graph Status

UI Graph Keeper did not run this session — **no UI changes** were made. All work was backend AI infrastructure (`src/lib/ai/providers/**` + tests + scripts + ADR doc + memory). The UI graph remains as-is.

## 7. Memory Graph Status

**Memory Keeper ran.** `mcp_memory-resums_open_nodes(["AIServicesLayer"])` confirmed the entity exists. Appended 4 new date-tagged observations via `add_observations`:

- ADR-0044 graduation summary with 5 sprint commits
- Trap: never reintroduce `gemini-1.5-flash` / `gemini-1.5-pro` (404 on v1beta as of 2026-06-21)
- Test architecture (hermetic + live smoke, runner, diagnostic, lab evidence)
- Parked follow-ups (geminiErrorMessage blandness + quota=0)

Shard backup written to `docs/memory/ai-services.json` matching the MCP write. No new relations created (existing `serves` edges to WorklogDomain/JobBoardDomain/ResumeDomain/InterviewDomain remain accurate).

## 8. Workflow Review Cadence

**Workflow Review Cadence:** ✅ on cadence — 2 days since last review (2026-06-19 18:00 UTC baseline). This session did NOT perform a workflow review (no `scripts/analyze-chat-exports.mjs` run, no rule edits shipped) — so no append to `workflow-reviews.json`.

## 9. User Practice Adoptions

User probed verbatim; selected **"Track live-smoke-pre-commit pattern as practice adoption."** Appended to `docs/chat-exports/analysis/workflow-change-log.json` under `userPracticeAdoptions`:

- **`id`: live-smoke-pre-commit** — "Live-smoke-before-commit verification pattern" — when committing a refactor crossing an external boundary, augment hermetic mocks with an env-gated live smoke that accepts happy-path OR 503/429 as green. Proven on ADR-0044 Day 5: caught decommissioned `gemini-1.5` fallback that no mock would have surfaced, remediated in-sprint. Default for any future PR touching an external integration. Replaces "hermetic-only test coverage on external-API boundaries."

Two candidates explicitly declined (no infra entries for the two new PS scripts).

---

**Sprint summary (5-day total): 5 commits landed locally, all 13 originally-named callers + `/api/ai/chat` migrated to the unified `ai.generate()` façade, 3 providers shipped (ollama / gemini-fast / gemini-pro), `onUsage` wired end-to-end, hermetic 860/860 + live 5/5, ADR Accepted. Pending: `git push` (user-authorized this session, deferred to next action).**
