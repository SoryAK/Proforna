# Session Handoff — 2026-06-16 13:05

## Current Sprint

**Worklog Procedures polish** (post-ADR-0030). This session shipped two small but pivotal sprints on top of yesterday's ADR-0030 procedureDoc landing: (1) the rose procedure badge in the category-dot slot for at-a-glance kind disambiguation in the notes list, (2) a 3-way Create picker on the worklog-notes toolbar that lets the user pick **note · procedure · event** at creation time. The kind-move salvage path was abandoned mid-plan in favor of fixing misclassification at the source.

## Last Completed Step

Two sprints landed and pushed/committed this session:

- **ADR-0030 follow-up — procedure badge** (commits `41381a4` + `c00e1ee`, on `origin/main`): rose `R` badge replaces the category dot for `kind="procedure"` rows in the notes list/grid/table.
- **ADR-0031 — 3-way Create picker** (commit `40d7969`, **local only**): a `DropdownMenu` replaces the toolbar's `+ New note` button on `/worklog/notes`; items are **New note** (cyan `FileText`), **New procedure** (rose `ListChecks`), **New event** (fuchsia `MapPin`).

Files touched in this final commit:

- [src/components/worklog/worklog-notes-view.tsx](src/components/worklog/worklog-notes-view.tsx)
  - Added `FileText, ListChecks, MapPin` to lucide imports; added `buttonVariants` to button import.
  - New handlers `handleNewProcedure` (mirrors `handleNewNote` with `kind: "procedure"` + `title: "Untitled procedure"`) and `handleNewEvent` (`router.push("/worklog/map?place=1")`).
  - Trigger button → `DropdownMenu` with 3 items.
- [src/components/worklog/worklog-map-view.tsx](src/components/worklog/worklog-map-view.tsx)
  - Imported `useRouter, useSearchParams` from `next/navigation`.
  - `useEffect` at mount: reads `searchParams.get("place")`, calls `setPlaceMode(true)` when `"1"`, then `router.replace("/worklog/map", { scroll: false })` to strip the param.

Persistence:

- [docs/adr/0031-three-way-create-picker.md](docs/adr/0031-three-way-create-picker.md) created (Status: Accepted).
- `/memories/repo/parked-ideas.md` — Kind Move (item #1 under ADR-0029 backlog) marked **SUPERSEDED via ADR-0031**.
- Memory Keeper — `WorklogDomain` entity gained 6 observations covering the picker decision, the map-view seam, the supersede chain over Kind Move, the deliberate non-replacement of the 6 other `+ New note` entry points, the commit hash, and the base-ui v2 `asChild`-not-supported trap.

Observable outcome: the user can now create a note, a procedure, **or** a free-floating event without leaving `/worklog/notes` (the events flow routes through `/worklog/map?place=1` so the existing `lat+lng+location` validator stays honored). The picker is mounted in a single place that covers both the list view and the inline reader.

Verification (E→T→C):

- `get_errors` clean on both files.
- `npm test`: **814 / 814** across 61 files.
- Browser smoke at `http://localhost:3000`: trigger renders as `+ New`, dropdown opens with 3 colored items, **New procedure** click → POST `/api/work-logs` returns `kind: "procedure"`, `title: "Untitled procedure"` → URL becomes `/worklog/notes/<id>`. **New event** click → URL becomes `/worklog/map` (param stripped) → "Click on the map to drop your event" banner visible.

## The "Live" Context

- Picker mount point: `worklog-notes-view.tsx` toolbar — single component covers `/worklog/notes` (list) AND `/worklog/notes/[id]` (reader). No second mount needed.
- Map place-arm seam: `?place=1` query param on `/worklog/map`, consumed once by `useEffect` then stripped via `router.replace`.
- POST whitelist for `kind: "procedure"` was already in place from ADR-0029 — no API change.
- The procedure smoke-test row from this session lives at `/worklog/notes/5fb7768b-823a-42db-8470-70714611d3f0` with title "Untitled procedure" — harmless test data, can be deleted via the reader.
- Recently-rejected: **Kind move** (`PATCH /api/work-logs/[id]/kind` + UI affordance). Griller pushback during planning surfaced misclassification was theoretical (1 procedure in DB; rose badge handled disambiguation). Cause-not-cure fix shipped instead. Stays parked with re-entry trigger raised to "real incidents in the wild."
- Recently-rejected: **Replace all 7 `+ New note` entry points**. Most callsites (grid empty-state, table empty-state, reader empty-state, home Quick Capture, command palette, Cmd+K) are contextually kind-locked. Audit sweep with no marginal value.

## Next Immediate Step

> "Pick up Worklog Procedures polish. ADR-0031 is committed locally (`40d7969`) but **not pushed** — confirm I should push first, then we can either (a) move to the next item in the procedures backlog (procedure templates / 'Duplicate this procedure' if I have 5+ procedures, or procedure folders, or archive integration parity for procedures), or (b) revisit Procedure-specific Properties rail tabs (was originally backlog candidate #1 before the picker pivot)."

## Unresolved Blockers

- [ ] **Push pending** — commit `40d7969` is local-only. Per Git Push Policy, await explicit user instruction before pushing. The badge sprint commits (`41381a4` + `c00e1ee`) are already on `origin/main`.
- [ ] **Test-data cleanup** — `/worklog/notes/5fb7768b-823a-42db-8470-70714611d3f0` ("Untitled procedure") was created during browser smoke. Harmless but the user may want to delete it.
- [ ] **Per-row Send menu / standalone export picker / 6 other `+ New note` entry points** — all parked under their own re-entry triggers in `parked-ideas.md`. Do NOT broaden the picker without a specific incident.

## Optional: Deferred Ideas

- **Make the toolbar picker discoverable from other surfaces** — e.g. a "+ New (anything)" entry in the command palette that re-uses the same handlers. Currently command palette is note-only. Re-entry trigger: user reports they wanted to create a procedure from Cmd+K and got a note.
- **Quick Capture kind toggle** — `home/worklog-quick-capture.tsx` is currently hard-wired to `kind: "note"`. A small radio above the textarea (note · procedure) would let captures be procedures from the home page. Same logic as the picker but inline. Re-entry trigger: user habitually creates procedures via Quick Capture and has to re-classify after the fact.
- **Per-row archive / unarchive in the procedures view** — ADR-0026 buckets work on procedures but `/worklog/procedures` has no archive view (parked-ideas.md item #4 under ADR-0029 backlog, untouched this session).
