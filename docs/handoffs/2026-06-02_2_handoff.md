# Handoff — 2026-06-02 (Session 2)

## Current Sprint
Separate Work History map from Job Search map — give each its own dedicated route and locked UI mode.

## Last Completed Step
Full separation implemented and verified clean:
- `WorkHistoryMap` wrapper (`src/components/work-history-map.tsx`) — locked to work-history mode
- `JobSearchMap` wrapper (`src/components/job-search-map.tsx`) — locked to job-search mode
- `/work-map` page → imports `WorkHistoryMap`
- `/job-search` page → imports `JobSearchMap`
- Mode-switcher button hidden in both `job-map-google.tsx` (map overlay) and the action bar in `job-map.tsx` when `lockedMode=true`
- `constants.ts`: "Work History" nav item now points to `/work-map` with `Map` icon
- `sidebar.tsx`: sub-expansion tracks `/work-map` and `/experience/*`
- MCP memory supervisor: renamed exe (`mcp-memory-supervisor.exe`) visible in Task Manager; `start-memory-server.ps1` updated to use renamed exe

## The "Live" Context
- Branch: `feature/interactive-resume-split-layout`
- Baseline commit before this session's work: `a04192a`
- All work-map split changes are **uncommitted** (staged next)
- `JobMap` now accepts `{ initialMode?, lockedMode? }` props — both optional, backward compatible
- `lockedMode=true` → passes `onToggleWorkHistory={undefined}` to google map component + hides Work History button in action bar

## Next Immediate Step
Session ended — all changes committed and pushed to main. Resume with any new feature or bug.

## Unresolved Blockers
- None from this session

## Parked Ideas
- VS Code Copilot feature request: chat checkpoints with selective deletion (see `/memories/repo/parked-ideas.md`)

## UI Graph Status
UI Graph Keeper audited `.github/ui/tokens.md`, `.github/ui/index.md`, `.github/ui/sidebar.md` — no new tokens or patterns introduced this session.

## Memory Graph Status
Memory Keeper audited session — no new entities warranted (structural routing change only, no new domain logic introduced).
