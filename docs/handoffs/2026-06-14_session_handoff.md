# Session Handoff — 2026-06-14 (rewrite, post-compaction)

> Supersedes the earlier 2026-06-14 draft. That draft was written
> mid-investigation and ended with "wait for two model pulls to finish."
> The investigation continued post-pull and reached real conclusions —
> this is the actual ending state.

## Current Sprint

**Cost-control infrastructure** for VS Code Copilot premium-request
burn. Ship local-LLM tooling that absorbs routine turns so Opus stays
reserved for architecture / Griller / multi-file refactors.

## Last Completed Step

Built and validated **two complementary local-LLM workflows**:

1. **Cline + Ollama gemma4:26b + codegraph MCP + memory MCPs** — for
   interactive turns that need codebase exploration mid-task. Validated
   pre-compaction: gemma4 successfully called `codegraph_search` then
   `codegraph_callers` on `useReaderRailState`.
2. **`scripts/ollama-codegen.ps1`** (new this session, post-compaction)
   — for closed-form spec → code-out → test-pass loops. No extension.
   Validated 2026-06-14 on `truncateToWordBoundary` + 9 vitest tests:
   **PASS in 1 attempt, 123.8s**.

- Files touched:
  - `scripts/ollama-codegen.ps1` (NEW — ~140 lines, untracked, not yet
    committed)
  - `src/lib/truncate-to-word-boundary.ts` + `.test.ts` (regenerated by
    the script on the final validation run; tests green)
  - `docs/workflows/orchestrator-executor-codegen.md` (NEW)
  - `/memories/resumsify-lessons.md` user-memory updated with the
    script section under "Local LLM Workflow Agent"
- Configs that survived the session:
  - Cline MCP settings:
    `%APPDATA%\Code\User\globalStorage\saoudrizwan.claude-dev\settings\cline_mcp_settings.json`
    — 4 MCP servers (codegraph stdio via `cmd /c`, memory-resumsify /
    memory-global / memory-user-profile SSE).
  - Continue config: `~/.continue/config.yaml` — codegraph wired but
    tools never fired in practice. Kept as fallback only.
  - `.clinerules` at repo root — written during the Cline polish pass.
    Still on disk. Probably should land in `.gitignore` if we decide
    not to share Cline rules across machines.

## Decision Log (this session)

- ✅ Cline + gemma4:26b + MCP = primary local-IDE chat agent for this
  repo. Replaces ~50–70% of routine Copilot turns.
- ✅ `ollama-codegen.ps1` = primary local-codegen tool when the spec is
  closed-form. Replaces the rest of the "you write the type, gemma
  writes the implementation" turns.
- ✅ 3-tier routing locked in:
  - **Tier 1 (free)**: Cline+gemma4 / `ollama-codegen.ps1`
  - **Tier 2 (cheap)**: Copilot picker → Gemini 3.5 Flash
  - **Tier 3 (premium)**: Copilot + Opus 4.7+
- ✅ `qwen3-coder:30b` ruled out for tool-call workflows (hybridized
  `<function=...></tool_call>` tag bug — chat template, not bridge).
  Keep on disk for autocomplete only.
- ❌ Continue extension as a primary client — MCP tools never fire.
  Cline owns this slot.
- ❌ Interactive Ollama REPL via `send_to_terminal` orchestration —
  works, but polling for "is gemma done?" is fragile vs the blocking
  HTTP call. `ollama-codegen.ps1` is the right shape.

## The "Live" Context

- **Working tree status** (verify with `git status` on resume):
  - Untracked: `scripts/ollama-codegen.ps1`,
    `docs/workflows/orchestrator-executor-codegen.md`,
    `docs/handoffs/2026-06-14_session_handoff.md` (this file),
    `.clinerules`.
  - Modified: `src/lib/truncate-to-word-boundary.ts`,
    `src/lib/truncate-to-word-boundary.test.ts` (regenerated by demo
    run; functionally identical to the manual round-1 paste).
- **HEAD before session**: `66a6d0a` (worklog-nav archive + folders
  disclosure). No commits landed this session yet — see "Next
  Immediate Step".
- **Memory supervisor**: NSSM service `MCPMemorySupervisor`, ports 3717
  / 3718 / 3719 verified listening at session end.
- **Codegraph index**: 569 files / 5994 nodes / 10061 edges / 16.20 MB.

## Next Immediate Step

Two-line resume:

> "Commit the local-LLM tooling — `scripts/ollama-codegen.ps1` +
> `docs/workflows/orchestrator-executor-codegen.md` + this handoff +
> `.clinerules` decision (commit or gitignore). E→T→C rhythm, no push.
> Then back to ADR-0027 worklog events when you're ready."

If `.clinerules` should NOT be checked in (per-machine config), add it
to `.gitignore` BEFORE the commit instead of staging it.

## Unresolved Blockers

- [ ] `.clinerules` policy decision — commit (share rules across
      checkouts) vs gitignore (per-machine). No strong preference; the
      file is mostly tool-call discipline restated.
- [ ] ADR-0027 worklog events is still **Proposed / PARKED**. Untouched
      this session. Do not modify without explicit "Accepted".
- [ ] Cline rules file path is `.clinerules` (singular, no extension).
      Confirm Cline still reads it — its docs sometimes flip between
      `.clinerules` and `.clinerules/` directory. Quick test if the
      file gets committed.

## Optional: Deferred Ideas

- Wrap `ollama-codegen.ps1` as an npm script
  (`"codegen:local": "pwsh ./scripts/ollama-codegen.ps1"`) once the
  argument-passing pattern is stable. Currently inline invocation is
  fine.
- Add a `-Model` matrix mode that runs the same spec against
  `gemma4:26b` and a future second model, reporting which iteration
  count + seconds for benchmark comparison.
- Add a Cline rules variant of the same spec template so the two paths
  produce comparable outputs from the same source-of-truth.

## UI Graph Status

UI Graph Keeper did not run — no `.tsx` files touched. Pure tooling
session.

## Memory Graph Status

Domain memory graph not modified — this session's output is tooling /
workflow knowledge (recorded to user-memory `resumsify-lessons.md` and
`docs/workflows/orchestrator-executor-codegen.md`), not Resumsify
domain logic. No new Memory Keeper entities warranted.
