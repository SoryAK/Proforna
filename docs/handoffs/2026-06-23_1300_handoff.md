# Handoff — 2026-06-23 13:00 (local)

Session ID: `77e25d9d-ca47-4795-b5f4-d8921afd0593`
Predecessor: [2026-06-23_1100_handoff.md](2026-06-23_1100_handoff.md)

## Current Sprint

Decompose the `src/components/ai-chat.tsx` god file (1026 LOC) into a slim
orchestrator + dedicated hooks + sibling presentation components, without
introducing behavior change.

## Last Completed Step

**Refactor complete. All 8 phases shipped, smoked, and documented.**

| # | Phase | SHA | god-file LOC |
|---|---|---|---|
| 0 | CSS dangling-import fix (pre-req baseline) | `6130bfc` | — |
| 1 | Constants + types extract → `src/lib/ai-chat-constants.ts` | `c8d7fa3` | 1026 → 934 |
| 2 | `useAiChatResize` → `src/hooks/use-ai-chat-resize.ts` (browser-verified) | `d526215` | 934 → 901 |
| 3 | `useAiChatModels` → `src/hooks/use-ai-chat-models.ts` | `1b306a9` | 901 → 846 |
| 4 | `useAiChatContext` → `src/hooks/use-ai-chat-context.ts` | `7323eee` | 846 → 798 |
| 5 | `useAiChatStream` → `src/hooks/use-ai-chat-stream.ts` (HIGH RISK, Path B) | `4b64bb7` | 798 → 674 |
| 6 | `useAiChatSlash` → `src/hooks/use-ai-chat-slash.ts` | `0d32757` | 674 → 624 |
| 7 | 6 sibling components → `src/components/ai-chat-{empty-state,message-list,slash-menu,context-chips,settings-panel,toolbar}.tsx` | `34a0f5c` | **624 → 281** |
| 8 | Slice manifest refresh → `docs/c-yard/ai-chat.json` | `9f52466` | docs only |

**Final delta: 1026 → 281 LOC (−745, −72.6%).** Vitest 966/971 baseline held
through every commit. End-to-end browser smoke against
`deepseek-r1:32b` via Ollama returned 1103 chars in 101.0s with full
markdown rendering, `AIProvenanceChip` hydrated
(`ollama:deepseek-r1:32b · 101.0s`), token meter `~299t`, and visible
context-slice plumbing (model output referenced profile data:
McDonald's, L3Harris, Siemens, PLC).

## The "Live" Context

- **`src/components/ai-chat.tsx`** (281 LOC, slim orchestrator). Owns
  state, refs, effects, threadContext useMemo, clearChat,
  toggleTurnCollapsed, 5 hook compositions, the `handleSlashKey`
  composer, and the JSX shell that wires the 6 sibling components.
- **`src/hooks/use-ai-chat-{resize,models,context,stream,slash}.ts`** —
  domain logic. The stream hook uses Path B (receives `messages` +
  `setMessages` as params, mirrors via internal `messagesRef` to keep
  `sendMessage` dep array stable across SSE chunks).
- **`src/components/ai-chat-{empty-state,message-list,slash-menu,context-chips,settings-panel,toolbar}.tsx`** — presentation siblings.
- **`docs/c-yard/ai-chat.json`** — slice manifest refreshed; god-file
  trap retired, replaced with "new behavior goes in a sibling hook or
  component" guardrail. `lastValidatedAt: 2026-06-23`, `lastValidatedCommit: 34a0f5c`.
- **`docs/workflows/decompose-god-file.md`** — new workflow recipe
  capturing the 8-phase pattern, the Path B hook signature convention,
  the composable-keydown `handleSlashKey` returns-boolean pattern, the
  `$msgP<N>` unique-variable commit-pattern lesson, and the
  "defer browser smoke to end of sequenced refactor" user practice.

## Next Immediate Step

No carryover from this sprint. Likely next directions, ranked by
freshness:

1. **Resume the ADR-0046 follow-ups** parked at end of prior session
   (toast surface for action errors, URL-aware ambient context, full
   mention-driven worklog targeting for `send-to-worklog`).
2. **Apply `decompose-god-file.md` to the next-largest client component**
   if one is over ~600 LOC (run `Get-ChildItem src/components -Filter
   *.tsx | ForEach-Object { @{ Path=$_.FullName; Lines=(Get-Content
   $_.FullName).Count } } | Sort-Object Lines -Descending | Select
   -First 5`).
3. **Wire the parked-ideas list** if you want a new feature surface.

Suggested resume prompt: *"What's the next largest god-file candidate
in `src/components/`? Apply `decompose-god-file.md` if anything is over
600 LOC."*

## Unresolved Blockers

None. All commits clean, dev server running on port 3000, vitest green
(966/971), browser smoke green.

## UI Graph Status

UI Graph Keeper audited `.github/ui/` (8 files: `global.md`, `index.md`,
`inventory.md`, `maps.md`, `resume.md`, `sidebar.md`, `tokens.md`,
`worklog.md`) — no new tokens or patterns introduced this session.
The refactor moved existing JSX between files without changing the
visual surface; browser smoke confirmed pixel-identical rendering.

## Memory Graph Status

Memory Keeper wrote 1 new entity: **AIChatPanelArchitecture**
(`entityType: ArchitecturalSystem`, 7 observations dated 2026-06-23
covering: slim orchestrator scope, 5-hook layout, 6-sibling layout,
Path B streaming-hook contract, composable-keydown `boolean` return
convention, retired god-file trap + replacement guardrail, live smoke
evidence at commit `34a0f5c`). Verified absence via `open_nodes(["AIChatPanel", "AIChatDomain"])` (empty) and discovery via
`search_nodes("ai-chat")` (no AIChat-named entities present, only
adjacent `AIServicesLayer`). 1 new relation:
`AIChatPanelArchitecture --consumes--> AIServicesLayer`. Shard
backup appended to `docs/memory/ai-services.json`; index entry added
to `docs/memory/index.json` shards map.

## Workflow Review Cadence

✅ on cadence — 3 days since last review (target: weekly). No
workflow-review actions ran THIS session, so no append to
`docs/chat-exports/analysis/workflow-reviews.json` is required.

## User Practice Adoptions

Probed — no new adoptions this session. (Three candidates surfaced and
offered:
1. `defer-browser-smoke-to-end-of-refactor`
2. `powershell-unique-commit-msg-var`
3. `decompose-god-file-recipe`.
User selected "no". Both technical patterns are still captured in the
new `docs/workflows/decompose-god-file.md` recipe for future-session
visibility; the change-log attribution layer was intentionally skipped.)
