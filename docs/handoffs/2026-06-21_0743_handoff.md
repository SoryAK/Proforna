# Handoff — 2026-06-21 0743 (Shipyard Phase 1 schema locked)

## Current Sprint

**Shipyard Phase 1 — Slice unification.** Phase 1 sprint kickoff ADR (ADR-0038) is Proposed. Phase 1 commit 1 (first slice + schema doc + empty `_index.json`) is cleared to ship in the next ETC cycle. Goal of the whole phase: replace memory graph + scattered traversal knowledge with one canonical primitive (`docs/c-yard/<slice>.json` topic-keyed manifests).

## Last Completed Step

Atomic ETC commit `8206053` (`docs(shipyard): ADR-0038 Proposed — Slice schema lock + Extractability NFR`) — 5 files, 129 insertions, 6 deletions:

- **NEW** [docs/adr/0038-slice-schema-and-extractability.md](../adr/0038-slice-schema-and-extractability.md) — locks D1 (`_index.json` adjacency list), D2 (user-memory for cross-cutting with version-bound vs codebase-bound boundary rule), D3 (Shipyard repo-only), D4 NEW (standalone-repo long-term home; Resumsify is Phase 1 incubator), Extractability NFR §5, G1=A (UI-surface granularity, split at >~15 surface files), G2 provisional (warn-and-serve with `staleSurfaces[]`), G3 provisional (`{ sessions: [{ id, turnExcerpt, toolCallIndex }] }`).
- **AMEND** [docs/adr/0037-shipyard-context-engineering-layer.md](../adr/0037-shipyard-context-engineering-layer.md) — Decision #7 (standalone-repo home commitment) + Con #6 (extractability tax during in-repo phase).
- **UPDATE** [docs/plans/shipyard-roadmap.md](../plans/shipyard-roadmap.md) — Phase 1 "Open decisions" section replaced with "Locked decisions (ADR-0038 Proposed, 2026-06-21)" enumerating all 7 locks + new "Long-term home" subsection.
- **UPDATE** [/memories/repo/parked-ideas.md](../../memories/repo/parked-ideas.md) — Shipyard top section refreshed; Phase 1 commit 1 cleared to ship; Shipyard repo extraction added as future parked work.
- **APPEND** [docs/chat-exports/analysis/workflow-change-log.json](../chat-exports/analysis/workflow-change-log.json) — `shipyard:adr-0038-proposed` self-attribution in `infraToolingChanges`.
- **APPEND** [docs/memory/shipyard.json](../memory/shipyard.json) — 3 observations mirroring MCP write to `ShipyardArchitecture`.

A separate post-commit edit (this turn) appends `standalone-product-framing-for-in-repo-experiments` to `userPracticeAdoptions` per the Practice Adoptions probe answer.

## The "Live" Context

- **ADR-0038 status**: Proposed. Will become Accepted at Phase 1 commit 5 along with ADR-0037, *after* the first slice round-trips through Phase 0 in a second session.
- **First slice target**: `docs/c-yard/worklog-editor.json`. Source material lives in [docs/memory/worklog.json](../memory/worklog.json) (densest existing shard).
- **Schema field names that must be generic** (Extractability NFR §5): `entryPoints`, `traversalRecipes`, `fileFingerprints`, `staleSurfaces`, `evidence`. Forbidden: any `worklog*`, `resumsify*`, or `c-yard*` substring in field names.
- **Phase 0 lookup stack additions** (to be wired in Phase 1 commit 3): `docs/c-yard/<slice>.json` reads FIRST, BEFORE `mcp_memory_search_nodes` / `codegraph_context`. Mechanism: update [.github/instructions/compliance.instructions.md](../../.github/instructions/compliance.instructions.md) Phase 0 stack.
- **Analyzer flag in flight**: `--mine-traversals <slice>` for [scripts/analyze-chat-exports.mjs](../../scripts/analyze-chat-exports.mjs). Heuristic: `(toolName, normalizedArgs)` tuples present in ≥2 reviewed sessions with no fallback `grep_search` within 3 turns → emit candidates to `docs/c-yard/_candidates/<slice>.candidates.json`.
- **9 unpushed commits on `main`** including `8206053`. Do NOT push without explicit instruction.

## Next Immediate Step

**Phase 1 commit 1 — first slice + schema doc.** Single ETC cycle:

1. Create `docs/c-yard/worklog-editor.json` (hand-authored from `docs/memory/worklog.json` content, structured per ADR-0038 schema: `entryPoints`, `traversalRecipes`, `fileFingerprints`, `evidence`). Slice contains `worklog-draft-system` and `tiptap-yjs-integration` as `entryPoints` rows (per G1 split-threshold rule, no separate slices yet).
2. Create `docs/c-yard/README.md` documenting the schema + the Extractability NFR rules inline.
3. Create `docs/c-yard/_index.json` with `{}` (empty adjacency map is valid).
4. Manual Phase 0 read trial: open the slice, verify content is usable for "show me the worklog editor architecture" before any tooling consumes it.
5. Commit message: `docs(shipyard): Phase 1 commit 1 — first slice + schema doc`.

Resume sentence: *"Let's ship Phase 1 commit 1 — author the first slice `docs/c-yard/worklog-editor.json` plus schema doc and empty index. Pull the source material from `docs/memory/worklog.json` and respect the Extractability NFR (no `resumsify*` or `worklog*` substrings in schema field names)."*

## Unresolved Blockers

- **None on the critical path.** Phase 1 commit 1 has no upstream dependency. ADR-0038 §G2/G3 provisional defaults explicitly defer hard policy to Phase 1.5 so commit 1 has no blocking design question.
- **Pre-existing technical debt** (not blockers, just background): 8 commits unpushed prior to `8206053` (now 9 total); Shipyard repo extraction is parked with no trigger yet; G2/G3 firm policy is parked under Phase 1.5.

## UI Graph Status

UI Graph Keeper not invoked this session — pure architectural decision sprint, zero `.tsx` files touched, no new tokens or patterns introduced.

## Memory Graph Status

`ShipyardArchitecture` (`ArchitecturalSystem`): 8 → 11 observations. Three new 2026-06-21 entries appended via `mcp_memory-resums_add_observations` covering (1) the four ADR-0038 decisions + Extractability NFR, (2) G1 granularity rule + G2/G3 provisional defaults, (3) Phase 1 commit 1 clearance. `open_nodes(["ShipyardArchitecture"])` ran first to confirm entity existed (returned 8 prior observations + `WorkflowSelfReviewSystem builds on` relation). Shard backup at [docs/memory/shipyard.json](../memory/shipyard.json) mirrored all 3 new observations.

## Workflow Review Cadence

✅ on cadence — 2 days since last review (baseline 2026-06-19). No workflow review performed this session; no entry appended to `workflow-reviews.json`.

## User Practice Adoptions

`standalone-product-framing-for-in-repo-experiments` (appended to `userPracticeAdoptions` in workflow-change-log.json). Captures the practice shift surfaced this session: explicitly treat the current repo as incubator/first tenant when building cross-cutting systems that could plausibly serve other tenants, with an Extractability NFR enforcing generic naming + configurable paths + no host-repo-specific assumptions from day one. Replaces the implicit "features live where they're born" default. Triggered by user's mid-decision insight that "Shipyard should be its own repo because this is a product of itself."

Also `shipyard:adr-0038-proposed` (already in `infraToolingChanges` from `8206053` self-attribution).

Phase 4 Composite Gate — Memory Keeper visible `open_nodes` proof ✅ + verbatim Practice Adoptions probe asked + user answered "yes" with adoption appended ✅.

## Phase 2 self-attribution backfill (still deferred, separate ETC cycle)

Prior commits not yet self-attributed in `workflow-change-log.json`:

- `dcbc721` — Handoff Architect field #9 + Phase 4 composite gate
- `d6cefb4` — Recipe updates (analyze-chat-exports defaults + mark-reviewed)
- `4ea628a` — Analyzer era-tagging + ledger gates
- `2cbb9c1` — Centralize Era Comparison in metrics-summary.md

Re-entry: a dedicated low-friction ETC cycle reading git log of `.github/instructions/` + `scripts/` since 2026-06-19 and appending one `agentRuleEdits` or `infraToolingChanges` entry per commit. Do NOT bundle with feature work.
