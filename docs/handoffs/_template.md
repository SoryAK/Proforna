# Session Handoff — YYYY-MM-DD HH:MM

> Copy this file to `YYYY-MM-DD_HHmm_handoff.md` and fill in each section.
> Delete this blockquote when starting a real handoff.

## Current Sprint

{The high-level goal we are working toward — what feature/initiative this session contributes to. One or two sentences.}

## Last Completed Step

{Exactly what was achieved in THIS session. Be concrete: file paths, commit hashes, what now works that didn't before.}

- Commits: `xxxxxxx`, `yyyyyyy`
- Files touched: `path/to/file.ts`, `path/to/other.tsx`
- Observable outcome: {what the user can now see/do}

## The "Live" Context

{Specific variables, active logic paths, or line numbers currently "warm" in memory. This is what saves the next session 20 minutes of re-discovery.}

- Key state: `viewPreset` in `job-map.tsx` ~line 656
- Active pattern: {e.g. "compact prop pattern for embedded pages"}
- Recently-read files: {paths}
- Recently-rejected approaches: {what NOT to try again and why}

## Next Immediate Step

{The exact sentence/prompt the user should paste to resume work. Write it as if you were them.}

> "Pick up the {feature}. Last we left off, {state}. Next is {concrete action}."

## Unresolved Blockers

- [ ] {Bug — file:line, symptom, hypothesis}
- [ ] {Missing info — what we need from the user before continuing}
- [ ] {Technical debt — what we cut corners on, in what file}
- [ ] {External dependency — waiting on what}

## Optional: Deferred Ideas

{Things explicitly punted from this session for future consideration — keeps them out of memory clutter but not lost.}

- {Idea + brief rationale}
