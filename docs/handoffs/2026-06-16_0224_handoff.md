# Session Handoff — 2026-06-16 02:24

## Current Sprint

ADR-0030 — **Structured Procedure Documents**. v1 implementation now feature-complete: procedureDoc Tiptap schema, kind-aware editor mounting, paste normalization, ProcedureLink table + API, Properties rail "Linked procedures" section, and markdown export rules. All ten units shipped linearly behind locked decisions L1–L3 (tools optional, auto-numbering + optional titles, flat step list).

## Last Completed Step

Closed Unit 10 (markdown export rules for procedureDoc) via TDD RED→GREEN, ran full E2E smoke against the live `f02c66b6-…` procedure note, and amended `docs/adr/0030-structured-procedure-documents.md` with the full Implementation Log + per-unit hash table. Sprint is **code-complete and lab-verified** — only push authorization remains.

- Commits this session (linear chain on `main`):
  - `1d8508c` — ADR-0030 Unit 10: Markdown export rules for procedureDoc (TDD)
  - `55d5f6c` — ADR-0030 Unit 9: Properties rail "Linked procedures" section
  - `0b62655` — ADR-0030 Unit 8: ProcedureLink API routes (TDD)
  - `020b461` — ADR-0030 Unit 7: paste normalization for procedure docs
  - `0951e47` — ADR-0030 Unit 6: procedure toolbar + commands + step CSS labels
  - `165d764` — ADR-0030 Unit 5: wrap-procedure-docs migration script
  - `310181a` — ADR-0030 Unit 4: mount procedureDoc end-to-end
  - `15834f5` — ADR-0030 Unit 3: Tiptap node specs
  - `d129e9d` — ADR-0030 Unit 2: pure schema spec lib
  - `96e946c` — ADR-0030 Unit 1: ProcedureLink schema + migration
- Files touched (Unit 10 only):
  - [src/lib/worklog/export/pm-to-markdown.ts](src/lib/worklog/export/pm-to-markdown.ts)
  - [src/lib/worklog/export/pm-to-markdown.test.ts](src/lib/worklog/export/pm-to-markdown.test.ts)
  - [src/lib/worklog/import/types.ts](src/lib/worklog/import/types.ts)
- Files touched (this turn — wrap):
  - [docs/adr/0030-structured-procedure-documents.md](docs/adr/0030-structured-procedure-documents.md) — Implementation Log appended
- Observable outcomes:
  - Procedure note renders with Step 1/2/3/4 positional CSS labels in read view.
  - Edit mode shows the procedure-aware toolbar: B / I / list / Tools / + Step / ↑ ↓ / Title / Save version.
  - Properties rail shows "Linked procedures" section (gated on `kind === "procedure"`) with Outgoing/Incoming/+ Add link.
  - Markdown export of the live procedure produces:
    ```
    # Reset BL Weigher load cell (smoke)

    ## Step 1

    Open the side panel and confirm power off.

    ## Step 2
    …
    ```
  - 5/5 locked-rule assertions pass against production data.
- Tests: **814 passing (61 files)** — vitest 4.1.0 node-only.

## The "Live" Context

- Live smoke note: `/worklog/notes/f02c66b6-6169-4946-bf9f-fec9f610b7b1` — kind=`procedure`, body=`procedureDoc`, category=`task` (orthogonal to kind, **not a bug**).
- Locked emission rules in Unit 10 (memorize before next pm-to-markdown work):
  - `procedureTitle` → `# <text>` (skip if empty).
  - `procedureTools` → `## Tools\n\n<body>` (skip if body whitespace-only).
  - `procedureStep` → `## Step N` or `## Step N — <title>` (U+2014 em dash). N is **positional** starting at 1.
  - Procedure-only nodes never appear in `droppedBlocks`.
- `ProseMirrorDoc.type` widened to `"doc" | "procedureDoc"` ([src/lib/worklog/import/types.ts](src/lib/worklog/import/types.ts)).
- Properties rail gating lives in [src/components/worklog/right-rail/tabs/properties-tab.tsx](src/components/worklog/right-rail/tabs/properties-tab.tsx#L127) (`log.kind === "procedure"`).
- TanStack Query keys introduced this sprint: `["procedure-links", noteId]`, `["worklogs-procedure-pool"]`.
- The note category badge (e.g. "Task") in the list view reads `category`, not `kind`. Both fields coexist on `WorkLog` — `kind` is the discriminator, `category` is the user-chosen label.

## Next Immediate Step

> "ADR-0030 v1 is code-complete + lab-verified, sitting on `main` at `1d8508c`. Push? (10 commits to land on origin/main)."

If the user authorizes push:

```pwsh
git -c gc.auto=0 push origin 1d8508c:main
```

(Direct ref push avoids the OneDrive y/n loop on local checkout/gc.)

## Unresolved Blockers

- [ ] **Push authorization** — Git Push Policy: do NOT push without explicit user "push" instruction. Currently 10 unpushed commits ahead of `origin/main`.
- [ ] (Pre-existing, not from this sprint) `docs/adr/0030-structured-procedure-documents.md` has 6 markdown-lint warnings (MD040 / MD026 / MD032) carried over from when the ADR was written. Out of scope — leave alone unless requested.

## Optional: Deferred Ideas

(All previously parked under ADR-0030 "Non-Goals" — re-flagged here so they don't get lost):

- **Per-run state** ("template + instance" model with a `ProcedureRun` table). Checkbox state currently lives in the document.
- **Drag-to-reorder steps** (Notion-style). Toolbar ↑/↓ buttons cover this for v1.
- **Step-aware version diff.** ADR-0017 diffs work on the flat ProseMirror tree today; "Step 2 changed" view is a v2 polish.
- **PWA offline draft sync for procedures** (currently shares the notes draft pipeline — should be fine, but worth a smoke once online support lands).

## Skills compliance log

- Phase 0 — Knowledge Orientation: codegraph/grep used for symbol lookups; locked emission rules surfaced before write.
- Phase 2.5 — TDD: Units 2 / 3 / 7 / 8 / 10 followed RED → GREEN → REFACTOR with verify steps.
- Phase 4 — Persistence: ADR amended (Implementation Log), Handoff written. UI Graph Keeper + Memory Keeper audit follows this commit.
- Verification-First Bias: ran full E2E smoke against live data BEFORE writing ADR amend / handoff.
