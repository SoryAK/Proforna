# Handoff — 2026-06-24 23:30

## Current Sprint

**Battle-tested review pipeline + ETRC rhythm.** This session continued yesterday's Tier A enforcement layer (commits 7c7202a + a6b7683 from the 22:00 handoff) and shipped a major rhythm upgrade: the bare `E→T→C` user-memory pattern is now `E→T→[auto-classify]→R-if-risky→C` with every commit auto-logged into a tier-bucketed queue for review-day batching. Tier A (auto-enforce existing rigor on push) + ETRC (auto-classify and selectively design-review on commit) are now both live.

## Last Completed Step

`6577e09` — `docs(review): ADR-0049 + ETRC rhythm workflow recipe + memory shard`.

Followed by Phase 4 close-out this turn: `mcp_memory-resums_open_nodes(["ReviewPipeline"])` ran (still 503, sticky from yesterday); shard `docs/memory/review-pipeline.json` updated with 5 new 2026-06-24 observations on ReviewPipeline; Practice Adoption probe asked verbatim — user selected "log all 5 candidates"; `workflow-change-log.json` appended with 1 new `userPracticeAdoptions` entry (`practice:etrc-rhythm-default`) and 4 new `infraToolingChanges` entries (lefthook post-commit hook, ETRC classifier, ETRC queue, R-local gemma4 design check).

ETRC live integration test passed end-to-end this session: every commit after `e739ef8` (the feature itself) was auto-classified by the new post-commit hook with no perceptual latency (0.24–0.36s).

## The "Live" Context

- 12 session commits total today (5 from the morning Tier A wrap + 5 new ETRC, plus 2 fix-ups); ZERO pushes.
- `e739ef8` ETRC feature commit is the only **risky**-tier commit logged today; R-local returned `skipped:empty-response` for it. Same outcome on `fb4638c` (the schema migration from yesterday). Two consecutive empty R-local responses is the strongest signal that v1 gemma4 prompt needs tuning — but the hard contract held (logged, joinable, recoverable).
- `docs/review-queue.jsonl` is populating in real time via the post-commit hook. Currently has 4 commit entries (a6b7683, e739ef8, 139d1e5, 6577e09) + 1 review entry. Gitignored, per-machine.
- `npm run review:queue` (with optional `--tier`, `--since`, `--json`) is the review-day reader.
- `npm run review:escalate <sha>` is the manual premium-subagent prompt formatter — pure formatter, no API call.
- Hooks installed: `.git/hooks/pre-push` (Tier A: review:phase auto-enforcement) + `.git/hooks/post-commit` (ETRC: classifier + queue logger + optional R-local spawn).
- MCP memory-resumsify is STILL in 503 "Single-client backing server already occupied" from yesterday's session. Per user memory note 2026-06-15 this is fixed VS Code-side only via Command Palette → **MCP: Restart Server** → memory-resumsify. The shard is canonical; today's 5 new observations live in `docs/memory/review-pipeline.json` and will sync to the live MCP graph after that restart.

## Next Immediate Step

**Take the new rhythm for a real spin.** Use a normal ETC cycle for any next feature work — the post-commit hook will auto-log and (for risky tier only) auto-spawn R-local. After 5–10 risky commits have accumulated, run `npm run review:queue --tier risky --json | jq '.[].reviews'` to inspect the R-local result distribution; if `skipped:empty-response` continues to dominate, iterate the `PROMPT_TEMPLATE` in `scripts/review-r-local.mjs`.

**Two priority follow-ups (small, both deferred to next session):**

1. **Reset MCP memory-resumsify**: Command Palette → MCP: Restart Server → memory-resumsify. Then a brief `add_observations` call against `ReviewPipeline` brings the live MCP graph into parity with the shard (5 new 2026-06-24 observations queued). Estimated 2 minutes total.
2. **Activate Dependabot** by making the first push since yesterday's `.github/dependabot.yml` shipped (commit 7c7202a). User explicitly held back pushes this session and last. When ready: `git -c gc.auto=0 push`.

**Optional pickup if the user wants to chip at parked work:**

- Tier B5 (Semgrep `--baseline-commit` mode) — pure config edit, ships in one ETC cycle, would unlock Tier B4 callsite-cleanup rule promotion.
- Tier B4 Shape C cleanup (toast-system-eligible callsites) — opportunistic, see `/memories/repo/parked-ideas.md` for the inventory.

## Unresolved Blockers

1. **MCP memory-resumsify 503** — sticky from yesterday. Shard write is the workaround (already done this session). Will resolve on next session start via Command Palette restart per user memory protocol.
2. **R-local v1 prompt returns `skipped:empty-response` on small risky diffs** — two-for-two so far (`fb4638c` + `e739ef8`). The contract holds (logged, joinable, escalate-able), but R signal is weak. Tuning is parked in ADR-0049 promotion candidates after 5–10 risky commits of data accumulate. Not blocking; classifier + queue + escalation paths all working.
3. **Dependabot not yet activated** — config shipped 7c7202a, awaits first push to remote HEAD. Zero pushes authorized today.

## UI Graph Status

UI Graph Keeper did NOT run this session — no `.tsx` files modified, no UI surface changes. All work was dev-infrastructure (scripts, lefthook config, npm scripts, JSON memory shard, ADR + workflow recipe docs). No `.github/ui/` audit was warranted.

## Memory Graph Status

Memory Keeper attempted `mcp_memory-resums_open_nodes(["ReviewPipeline"])` → returned 503 sticky (same VS Code-side blocker as yesterday's session, documented in user memory note 2026-06-15). Fell back to direct shard write per rebuild protocol — `docs/memory/review-pipeline.json` updated with 5 new 2026-06-24 observations on the `ReviewPipeline` entity covering: ETRC rhythm shipped (commits e739ef8 + 139d1e5, ADR-0049), tier heuristics (mechanical, validated 2/7 risky against today's session), first-attempt failures (Windows cmd.exe caret-eating, lefthook `{ref}` non-resolution on post-commit, gemma4 v1 empty-response on small structural diffs), queue file convention (gitignored append-only JSONL, per-machine, review-day-only artifact), workflow recipe pointer (`docs/workflows/etrc-rhythm.md`). MCP live-index parity deferred to next session after Command Palette → MCP: Restart Server.

## Workflow Review Cadence

✅ on cadence — 4 days since last review (target: weekly). This session did NOT perform a workflow review (it shipped feature + infra work driven by the user's strategy question about ETRC); `docs/chat-exports/analysis/workflow-reviews.json` was NOT appended.

## User Practice Adoptions

Probed verbatim via `vscode_askQuestions` — user selected **"Yes — log all 5 candidates (4 infra + 1 practice)"**. Appended to `docs/chat-exports/analysis/workflow-change-log.json`:

**userPracticeAdoptions:**

- `practice:etrc-rhythm-default` — ETRC ratcheted from yesterday's E→T→R→C (manual review:phase per commit) to E→T→[auto-classify]→R-if-risky→C (mechanical post-commit classifier + selective local-R, always commit-and-queue, no fix-in-loop branch).

**infraToolingChanges:**

- `infra:lefthook-post-commit-hook` — added non-blocking post-commit stage to `lefthook.yml`.
- `infra:etrc-classifier` — `scripts/review-classify.mjs` mechanical tier classifier.
- `infra:etrc-queue` — `docs/review-queue.jsonl` append-only queue + `scripts/review-queue-read.mjs` reader + `scripts/review-escalate.mjs` formatter + 3 new npm scripts (`review:classify`, `review:queue`, `review:escalate`).
- `infra:r-local-gemma4-design-check` — `scripts/review-r-local.mjs` local Ollama gemma4:26b design-only R for risky-tier commits, detached + healthcheck-gated + 2-min capped.

## Session Commit Trail (this session — both halves)

**Morning half — Tier A wrap (covered in 22:00 handoff):**

1. `fb4638c` — perf(schema): add @@index([userId]) on WorkHistory, Contact, Residence
2. `efdf94d` — docs(workflow): fix stale ADR-0047 cross-ref in commit-based-review
3. `64c108e` — chore(scripts): add Prisma migration diagnostic utilities
4. `00ba95f` — chore(scripts): EXPLAIN ANALYZE helpers for mention-search index verification
5. `7c7202a` — feat(review): ship Tier A review pipeline — lefthook + Semgrep + Dependabot
6. `a6b7683` — docs(review): ADR-0048 + workflow recipe + change-log + memory shard for Tier A

**Evening half — ETRC rhythm (this turn):**

1. `e739ef8` — feat(review): ETRC rhythm — post-commit classifier + queue + local-R gemma4 design check  [risky tier — auto-classified live]
2. `139d1e5` — fix(review): drop {ref} template from post-commit hook — lefthook only resolves it for pre-push  [trivial tier]
3. `6577e09` — docs(review): ADR-0049 + ETRC rhythm workflow recipe + memory shard  [trivial tier]

**9 commits this session. ZERO pushes.** User has held back pushes both today and yesterday — Dependabot still dormant until that changes.

## References

- ADR-0047 — three-column review pipeline (what fires on review day).
- ADR-0048 — Tier A enforcement layer (auto-runs review:phase on push).
- ADR-0049 — **ETRC rhythm and commit queue** (this session; the rhythm + classifier + queue + local-R design check).
- `docs/workflows/etrc-rhythm.md` — full workflow recipe (day-to-day, review-day, adding triggers, tuning R-prompt).
- `docs/workflows/add-review-enforcement-layer.md` — recipe extended with post-commit hook sub-recipe.
- `docs/memory/review-pipeline.json` — 5 new 2026-06-24 observations on `ReviewPipeline`.
- `/memories/repo/parked-ideas.md` — Tier B/C/D roadmap (unchanged this session, but R-prompt v2 + TDD hard-block ratchet + classifier escape-hatch trailer + queue auto-rotation are NEW promotion candidates documented in ADR-0049 §References).
- Commits `e739ef8`, `139d1e5`, `6577e09` — the ETRC implementation that this handoff closes out.
