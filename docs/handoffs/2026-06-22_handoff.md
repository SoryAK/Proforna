# Handoff — 2026-06-22 — ADR-0046 Phase D wrap + ai-chat slice manifest

## 1. Current Sprint

**ADR-0046 (AI Chat Context-Awareness and Rich Actions)** — Accepted 2026-06-22. This session wrapped Phase D (rich code-block actions on streamed assistant messages) across three ETC commits, then hand-authored the AI-chat surface slice manifest while context was at maximum freshness.

## 2. Last Completed Step

Five commits landed today:

| Commit  | Phase / Scope |
|---------|---------------|
| `61f6151` | D.1 — `react-markdown` + `remark-gfm` + `rehype-sanitize` + `shiki` wiring, code-block component, copy button. |
| `ea657c9` | D.2 — `resolveActionTarget`, 3 action API routes (`send-to-worklog`, `add-to-job-notes`, `save-as-bullet`), ambient context fields on `/api/ai/context`. |
| `22215ce` | D.3 — per-turn mention snapshot, `threadContext`+`pageContext` via closure, `AIChatCodeBlock` toolbar wired to 3 ActionButtons, `AIChatActionPicker`. |
| `6dc5ef0` | `docs(c-yard)`: ai-chat slice manifest (7 entryPoints, 16 fileFingerprints, 3 traversalRecipes) + first cross-slice adjacency edge in `_index.json`. |
| `72101e2` | `docs(memory)`: shard backups for `AIServicesLayer` (+4 obs) and `ShipyardArchitecture` (+2 obs). |

## 3. The "Live" Context

- **ADR file warm:** [docs/adr/0046-ai-chat-context-awareness-and-rich-actions.md](docs/adr/0046-ai-chat-context-awareness-and-rich-actions.md) — Status flipped to Accepted at top of file.
- **D.3 components warm:** [src/components/ai-chat.tsx](src/components/ai-chat.tsx) (pageContext state, turnMentions snapshot, threadContext useMemo); [src/components/ai-chat-code-block.tsx](src/components/ai-chat-code-block.tsx) (3 ActionButtons + picker, `SUCCESS_PILL_MS = 1800`); [src/components/ai-chat-message-markdown.tsx](src/components/ai-chat-message-markdown.tsx) (forwards threadContext/pageContext via closure inside `code` override); [src/components/ai-chat-action-picker.tsx](src/components/ai-chat-action-picker.tsx) (NEW, ~180 LOC, Dialog-wrapped, base-ui pattern with `setTimeout` focus).
- **D.2 backend warm:** [src/lib/ai-chat-action-target.ts](src/lib/ai-chat-action-target.ts) (resolver, ambient > thread precedence); [src/app/api/ai/context/route.ts](src/app/api/ai/context/route.ts) (now returns `{ slices, systemPrompt, ambient }` with 24h `updatedAt` heuristic for `activeWorklog`); [src/app/api/ai/actions/send-to-worklog/route.ts](src/app/api/ai/actions/send-to-worklog/route.ts), [src/app/api/ai/actions/add-to-job-notes/route.ts](src/app/api/ai/actions/add-to-job-notes/route.ts), [src/app/api/ai/actions/save-as-bullet/route.ts](src/app/api/ai/actions/save-as-bullet/route.ts).
- **Slice manifest warm:** [docs/c-yard/ai-chat.json](docs/c-yard/ai-chat.json) — 7 entryPoints, ~30 knownTraps, refreshHistory tied to this session id.
- **Test baseline:** 957 passed | 5 skipped (held stable across all three D commits).

## 4. Next Immediate Step

Two complementary follow-ups surfaced but were deferred (neither blocks acceptance):

> *"Run the live browser smoke for ADR-0046 Phase D — focus on the 3 code-block action paths (send-to-worklog short-circuit happy path, add-to-job-notes via ambient `activeJob`, save-as-bullet via mention) with the dev server warm. Confirm the 1800ms success pill renders and the picker keyboard nav (Arrow/Enter/Esc) feels right. Visual ergonomics on the picker positioning is the highest-risk remaining unknown."*

Also queued: the **`author-slice-manifest.md` workflow recipe** — proposed but NOT yet greenlit. Worth writing before the next slice (`contacts.json` or `asset-library.json`) so the consolidated-entryPoint discipline isn't relearned.

## 5. Unresolved Blockers

None. Two deliberate parkings recorded as `knownTraps` in the slice manifest:

- **`send-to-worklog` v1 short-circuit** — resolver always returns `activeWorklog`; full mention-driven worklog targeting deferred until users hit the limitation.
- **No toast layer** — action failures surface as `console.error` only. Pill renders on success but error path is silent UI.

Dev server is exit-1 from a prior turn; restart when smoke verification begins.

## 6. UI Graph Status

UI Graph Keeper did NOT run this session. Rationale: surface-specific patterns (base-ui `DropdownMenuItem` onClick discipline, mention `/` convention, action precedence, code-block toolbar layout, Dialog-focus pattern) are captured more durably in [docs/c-yard/ai-chat.json](docs/c-yard/ai-chat.json)'s `knownTraps` than in a parallel `.github/ui/ai-chat.md` would be. No new global tokens introduced — only Tailwind arbitrary selectors (`[&_p]:my-1.5` style) which the slice manifest flags explicitly because `@tailwindcss/typography` is NOT installed. No reusable layout primitives to promote to `global.md`.

## 7. Memory Graph Status

Memory Keeper appended 4 observations to **`AIServicesLayer`** (verified via `mcp_memory-resums_open_nodes(["AIServicesLayer", "ShipyardArchitecture"])` pre-write; `add_observations` returned the full appended list): ADR-0046 Phase D commit chain + test baseline; action resolution precedence + v1 short-circuits + WorkHistory.accomplishments reuse + console-error-only failure path; ambient 24h heuristic limitation + route test coverage; slice manifest summary with knownTrap inventory. Memory Keeper appended 2 observations to **`ShipyardArchitecture`**: second slice authored (validates G1=A UI-surface granularity under pressure — final entryPoint count 7); first cross-slice adjacency edge written (confirms ADR-0038 D1=B). Shard backups mirrored to [docs/memory/ai-services.json](docs/memory/ai-services.json) and [docs/memory/shipyard.json](docs/memory/shipyard.json), committed as `72101e2`.

## 8. Workflow Review Cadence

Workflow Review Cadence: ✅ on cadence — 3 days since last review (last `reviewedAt` = 2026-06-19T18:00:00Z). No review performed this session; no append to `workflow-reviews.json`.

## 9. User Practice Adoptions

User Practice Adoptions: probed — user greenlit two new entries, appended to `workflow-change-log.json`:

- **`slice-while-the-iron-is-hot`** (userPracticeAdoptions stream) — hand-author the slice manifest immediately after a feature surface lands, while design intent is still freshly converged in working memory. Replaces the implicit "slice-authoring-deferred-to-Layer-2-mining-only" default. First proof: `ai-chat.json` shipped in `6dc5ef0` within minutes of ADR-0046 D.3 (`22215ce`) landing.
- **`shipyard:first-cross-slice-adjacency-edge`** (infraToolingChanges stream) — `docs/c-yard/_index.json` gained `{ ai-chat: { reads-from: [worklog-editor/version-history] } }` as the first real cross-slice edge. Validates ADR-0038 D1=B (adjacency-file approach) by load-bearing use.

Self-attribution: this session shipped no `.github/instructions/` edits, so no `agentRuleEdits` entry.
