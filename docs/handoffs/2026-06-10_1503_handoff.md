# Session Handoff — 2026-06-10 15:03

## Current Sprint

ADR-0024 (Inline Reader-First Notes Surface) wrap + ADR-0023 Unit 3.1 (Tags lift into rail). Both shipped end-to-end this session. Pipeline now back to a clean state ahead of the next ADR pickup.

## Last Completed Step

Shipped **ADR-0024 Unit 5** and **ADR-0023 Unit 3.1**. The drawer-only read shim is gone, the rail Tags tab is live, and the dual-surface inline-field pattern has a canonical template (`InlineTagsField`) ready to drive the parked Assets/Tools lift.

- Commits (this session, in order):
  - `f02f0a7` — feat(worklog): ADR-0023 Unit 3.1 — lift Tags into right-rail tab via InlineTagsField
  - `6377802` — chore(worklog): ADR-0024 Unit 5 — delete orphan WorklogNoteReadView, supersede ADR-0015 Phase 5
- Files touched / created:
  - NEW: [src/components/worklog/inline-tags-field.tsx](src/components/worklog/inline-tags-field.tsx) — canonical dual-surface template
  - EDIT: [src/components/worklog/right-rail/tabs/tags-tab.tsx](src/components/worklog/right-rail/tabs/tags-tab.tsx) — renders `<InlineTagsField hideLabel>`
  - EDIT: [src/components/worklog/right-rail/worklog-reader-right-rail.tsx](src/components/worklog/right-rail/worklog-reader-right-rail.tsx) — added `activeLog` / `onUpdateActiveLog` / `tagSuggestions` props + dispatch
  - EDIT: [src/components/worklog/worklog-note-reader.tsx](src/components/worklog/worklog-note-reader.tsx) — Tags inline block replaced with `<InlineTagsField className="xl:hidden">`; AssetPicker auto-tag merge now reads `log.tags ?? ""` (server canonical) and writes `{assetIds, tags}` in a single onUpdate patch
  - EDIT: [src/components/worklog/worklog-notes-view.tsx](src/components/worklog/worklog-notes-view.tsx) (~line 683) — passes `activeLog` / `onUpdateActiveLog` / `tagSuggestions` to rail
  - EDIT: [src/components/worklog/worklog-notes-and-reader.tsx](src/components/worklog/worklog-notes-and-reader.tsx) (~line 258) — same wiring as route view
  - DELETED: `src/components/worklog/worklog-note-read-view.tsx` (was the last drawer-only read shim, zero callers)
  - EDIT: [docs/adr/0015-worklog-notes-document-manager.md](docs/adr/0015-worklog-notes-document-manager.md) — Phase 5 banner now reads "Superseded by ADR-0024 — Inline Reader-First Notes Surface"
  - EDIT: `.Manual/note-to-note-linking.md` — bullets 9 & 12 dropped dead `WorklogNoteReadView` reference
  - EDIT: [docs/memory/worklog.json](docs/memory/worklog.json) — InlineTagsField entity created, WorklogNoteReadView removed, WorklogReaderRightRail/WorklogDomain observations refreshed, dead relations dropped
  - EDIT: [docs/memory/index.json](docs/memory/index.json) — `InlineTagsField` added to shards map, `WorklogNoteReadView` removed, `last_updated` bumped to 2026-06-10
  - EDIT: `/memories/repo/parked-ideas.md` — Assets/Tools entry refined to **inline-section shape** (one rail tab with stacked scrollable sections, Tolaria-style)
- Observable outcome: Tags now live in the rail's Tags tab on xl+ and inline `xl:hidden` below xl; AssetPicker auto-tag merge no longer races; the codebase has zero references to `WorklogNoteReadView`; ADR-0015 Phase 5 cleanly hands off to ADR-0024.

## The "Live" Context

- **Canonical template:** [src/components/worklog/inline-tags-field.tsx](src/components/worklog/inline-tags-field.tsx). PersistKey = `worklog:draft:${log.id}:tags`. Reads `draftMap.tags` → `readAutosaveDraft<string>(tagsKey)` → `log.tags ?? ""`. Writes `onUpdate({ id: log.id, tags: v || null })`. CSV↔array bridge for `<TagInput>`. **Use this as the model for `InlineAssetsField` / `InlineToolsField` when the parked ADR is picked up.**
- **Dual-surface dispatch:** [src/components/worklog/right-rail/worklog-reader-right-rail.tsx](src/components/worklog/right-rail/worklog-reader-right-rail.tsx) `renderTabBody(tabId, noteId, currentPlainText, activeLog, onUpdateActiveLog, tagSuggestions)` — extend signature here when adding more autosave-aware tabs.
- **AssetPicker race fix:** [src/components/worklog/worklog-note-reader.tsx](src/components/worklog/worklog-note-reader.tsx) — `(log.tags ?? "").split(",")` is the server-canonical read; merge with new asset names then single `onUpdate({ id, assetIds, ...(mergedTagsPatch ?? {}) })`. Same race shape as the editor mention-merge — accepted, documented inline.
- **Memory graph:** Live MCP graph for scope `memory-resums` has 3 entries refreshed (`WorklogReaderRightRail`, `WorklogDomain`, `InlineTagsField`). `WorklogNoteReadView` is **not** deletable through MCP today (see Unresolved Blockers); the shard backup is canonical and tombstones it via removal.
- **Drift to be aware of:** `WorklogReaderDrawer` was deleted in ADR-0024 Unit 3 (`81aadfc`) but its entity still lives in [docs/memory/worklog.json](docs/memory/worklog.json). Out of scope for Unit 5; flagged below.
- **Git state:** 12 commits ahead of `origin/main`. **No push** per user policy.
- **Recently-read files (warm in cache):** the 4 worklog component files above, the 2 right-rail files, both ADR docs, both memory shard files.
- **Recently-rejected approaches:** retrying MCP `delete_entities` / `delete_relations` / `add_observations` against `WorklogNoteReadView` after the byte-96 JSON parse error reproduces deterministically across `Restart-Service MCPMemorySupervisor` — do not re-attempt without first investigating the patch script.

## Next Immediate Step

Two options for the next session — user picks:

> (a) "Pick up the parked Assets + Tools rail lift. Per the refined parked-ideas entry, design it as ONE scrollable rail tab containing stacked inline sections (`InlineTagsField`, `InlineAssetsField`, `InlineToolsField`) — Tolaria-style. Start with The Griller (3 questions), then Architectural Reviewer for the section-ordering / picker-in-320px-rail trade-offs, then ADR-0025 before any code."

> (b) "Push the 12 ahead-of-origin commits to `origin/main` (use `git -c gc.auto=0 push` to dodge the OneDrive y/n loop)."

## Unresolved Blockers

- [ ] **MCP tool bug — `delete_entities` / `delete_relations` / `add_observations` fail on `WorklogNoteReadView`** with `Unexpected non-whitespace character after JSON at position 96 (line 1 column 97)`. Reproduces across `Restart-Service MCPMemorySupervisor`. Other entities accept writes fine in the same session. Worked around by removing the entity from the shard backup; the live MCP graph still has a ghost entry until investigated. Likely culprit: a stray character introduced by an earlier patch in `scripts/patch-mcp-memory.js` or in the entity's stored observations. Re-run `npm run memory:patch:check` first; if clean, the bug is in the entity payload itself and may need a manual SQLite/file purge of the MCP store.
- [ ] **Shard drift — `WorklogReaderDrawer` lingers in [docs/memory/worklog.json](docs/memory/worklog.json)** after Unit 3's code deletion (`81aadfc`). Out of scope for Unit 5. Bundle into the next docs/memory commit, or sweep when the parked Assets/Tools ADR lands.
- [ ] **`Improvents.md` typo at workspace root** — pre-existing, not from this session, noted for housekeeping.

## Optional: Deferred Ideas

- **Assets/Tools inline-section rail tab (PARKED)** — `/memories/repo/parked-ideas.md`. Re-entry criteria: user explicitly requests, AND `InlineTagsField` has logged ≥1 week of stable usage with no autosave race regressions.
- **Improvement (low-priority):** consider promoting the AssetPicker tag-merge bridge into a tiny pure helper (e.g. `mergeAssetTagsCSV(existingCSV, newAssetNames): string | null`) when the parked ADR lifts Assets — it'll need a unit test under Phase 2.5 TDD anyway, so no point introducing it before then.

---

### UI Graph Status

UI Graph Keeper not invoked — no UI tokens or patterns introduced this session. Both surface changes (rail Tags tab + inline `xl:hidden` block) reuse existing primitives (`TagInput`, `useAutosaveField`, `useWorklogDrafts`).

### Memory Graph Status

Memory Keeper audited session and wrote three live updates to scope `memory-resums`:

- `WorklogReaderRightRail` — refreshed observations to drop the placeholder Tags-tab note and add the Unit 3.1 + ADR-0024 wrap notes.
- `WorklogDomain` — appended four observations: ADR-0023 Unit 3.1 ship, ADR-0024 ship, parked Assets/Tools refinement, plus the existing Unit 3 mounting note kept verbatim.
- `InlineTagsField` — created with 4 observations (purpose, persistKey contract, dual-surface usage, canonical-template designation).

Shard backups in [docs/memory/worklog.json](docs/memory/worklog.json) and [docs/memory/index.json](docs/memory/index.json) updated to match. **Blocked write:** `WorklogNoteReadView` could not be deleted from the live MCP graph (see Unresolved Blockers); the shard backup is the source of truth and no longer references it.
