# Handoff — 2026-05-22 02:18 (local)

## Current Sprint
Worklog editor rebuild — Phase 2 (rich editor polish):
- **2a** slash commands ✅ (prior session)
- **2b** inline body photos ✅ (this session)
- **2c** read-only renderer + Edit button ✅ (this session)
- **2d** TBD: server-side autosave hardening / draft sync polish
- **2e** TBD: collab/presence (only if user opts in)

## Last Completed Step
Phase 2c shipped + build fix:
- New `src/components/worklog/worklog-note-view.tsx` — hand-rolled ProseMirror JSON renderer (no Tiptap, no Y.js). Supports paragraph, heading, blockquote, codeBlock, hr, bullet/ordered/task lists, photo (with `<Image unoptimized>` + figcaption), shiftBlock, moodBlock, tag, hardBreak, marks (bold/italic/code/strike/underline/link). Falls back to `<pre>` on legacy plain-text-only notes.
- `src/components/worklog/worklog-note-reader.tsx` — added `editMode` state (auto-true for empty notes, false otherwise), Edit/Done button next to body; Done flushes editor before exiting.
- Build fix: `formatShiftWindow` isn't exported from `@/lib/worklog-shifts` — inlined the formatter inside `worklog-note-view.tsx` (matches the one in `prosemirror-to-text.ts`).

## "Live" Context
Files touched this session (uncommitted):
- `prisma/schema.prisma` — `WorkLogPhoto.source` column + `@@index([workLogId, source])`
- `prisma/migrations/20260522050000_add_worklog_photo_source/migration.sql` — applied via `db execute` + `migrate resolve --applied` (schema drift on prior migrations forced manual application).
- `src/app/api/work-logs/photos/route.ts` — `MAX_PER_LOG_PANEL=6`, `MAX_PER_LOG_BODY=20`; `?source=panel|body|all` filter; `source` form field on POST.
- `src/app/api/work-logs/photos/reconcile/route.ts` — new; `POST {workLogId, source:"body", keepIds[]}` prunes orphans.
- `src/lib/worklog/tiptap/photo-node.ts` — new Tiptap node `photo`, atom/block/draggable.
- `src/components/worklog/photo-node-view.tsx` — React NodeView with debounced caption PATCH.
- `src/lib/worklog/photo-upload.ts` — `uploadBodyPhoto`, `patchBodyPhotoCaption`, `reconcileBodyPhotos`.
- `src/lib/worklog/tiptap/slash-commands.tsx` — accepts `items` override.
- `src/components/worklog/worklog-editor-toolbar.tsx` — accepts `slashCommands` prop.
- `src/components/worklog/worklog-editor.tsx` — PhotoNode registered, `/photo` slash + Insert (+), paste/drop image handling, hidden file input, `reconcileBodyPhotos` on flush + unmount.
- `src/lib/worklog/prosemirror-to-text.ts` — `photo` case → `[Photo: caption|alt|image]`.
- `src/components/worklog/worklog-note-view.tsx` — new read-mode renderer.
- `src/components/worklog/worklog-note-reader.tsx` — editMode state, view/edit toggle button.

Body photo cap: **20** (independent from panel cap of 6).
Photos source discriminator: `"panel"` (default, disclosure UI) vs `"body"` (inline in editor).

## Next Immediate Step (resume prompt)
> "Continue worklog rebuild. Test results from Phase 2b/2c: [paste results]. If green, commit Phase 2b+2c and move to Phase 2d (server autosave hardening). If anything's broken, fix first."

## Unresolved Blockers / Open Questions
1. **Untested by user yet** — Phase 2b (photo upload via /photo, paste, drop, caption mirror, reconcile-on-close) and Phase 2c (read mode shows old notes; Edit/Done toggle). User saw the read-mode empty-content issue on the previous editor; 2c is the fix but needs confirmation it now renders old notes.
2. **Schema drift on prior migrations** — `prisma migrate dev` refused; future migrations may need the same manual workaround (`db execute` + `migrate resolve --applied`). Consider a `prisma migrate resolve --rolled-back` or fresh shadow DB pass when convenient.
3. **EPERM on `prisma generate`** keeps recurring — `query_engine-windows.dll.node` gets locked by long-running node processes. Workaround: `Rename-Item ... .old_<rand>` before `prisma generate`. Already in user memory.
4. **No commits yet this session** — everything is in working tree. User has not said "push" or "commit"; do not auto-commit.
5. **Manual entry / ADR not written** — operating procedure says wait until user confirms tests pass before triggering Manual Engineer / ADR Author for Phase 2b/2c.

## Testing Checklist for Next Session
**Phase 2c (read mode)**
- [ ] Open existing note → content renders immediately, no Y.js spin-up
- [ ] Empty note → opens directly in edit mode
- [ ] Click Edit → editor mounts, content matches what was shown in view
- [ ] Click Done → returns to view, edits persisted
- [ ] Switch notes → resets back to view (key-based remount)
- [ ] Legacy text-only notes (no `contentJson`) → render via `<pre>` fallback

**Phase 2b (inline photos)**
- [ ] Type `/photo` → file picker → image embeds with caption input
- [ ] Insert (+) toolbar → Photo entry → same behavior
- [ ] Drag image file onto editor → uploads at drop position
- [ ] Paste screenshot → uploads at cursor
- [ ] Type caption → ~600 ms → DB `WorkLogPhoto.caption` updated
- [ ] Delete image node + close note → file removed from `/public/uploads/worklog/` + row deleted
- [ ] Existing Photos disclosure section → only panel photos, body images hidden
- [ ] Try 21st body photo → server rejects with cap-exceeded message
