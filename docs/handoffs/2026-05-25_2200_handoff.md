# Handoff — 2026-05-25 22:00 (local)

**Session Start:** 2026-05-25 (resumed from 2026-05-22_0218 handoff)

## Current Sprint
Phase D — Notable WorkLog → Career Event promotion UI ("the worklog's core value proposition")

## Last Completed Step

### Phase D shipped (uncommitted):
1. **`src/components/worklog/promote-to-event-dialog.tsx`** — NEW FILE, complete.
   - Dialog pre-filled from the worklog (title, date, positionId)
   - Fields: title, category (select), description (STAR narrative textarea), metrics
   - On confirm: `POST /api/work-history/{positionId}/events` → `PATCH /api/work-logs/{logId}` with `promotedToCareerEventId`
   - Invalidates `["worklogs"]` + `["career-growth"]` query caches
   - Uses `@base-ui/react` Dialog pattern (no `asChild`, no `onOpenAutoFocus`)

2. **`src/components/worklog/worklog-note-reader.tsx`** — modified:
   - `Trophy` import added from lucide
   - `PromoteToEventDialog` import added
   - `promoteOpen` state added (line ~231)
   - Promote button added in header action row — only visible when `log.isNotable && log.positionId && !log.promotedToCareerEventId`
   - "Promoted" badge shown when `log.promotedToCareerEventId` is set
   - `<PromoteToEventDialog>` rendered at bottom inside `<>` fragment wrapper (build error fix: was outside return, wrapped in fragment to resolve "Expected ',' got '{'" Turbopack parse error)

3. **`src/components/position-worklog-tab.tsx`** — modified:
   - `PromoteToEventDialog` imported
   - `promotingLog: WorkLog | null` state added
   - "Phase D promotion note" placeholder banner replaced with live action banner — lists each unpromoted notable entry by title with its own Promote button
   - Per-row Trophy icon button added in action area (only for `l.isNotable && !l.promotedToCareerEventId`)
   - Shared `<PromoteToEventDialog>` rendered at bottom driven by `promotingLog` state

4. **`src/components/worklog/worklog-page.tsx`** — `tagSuggestions` memo added (from prior session, already in this commit baseline)

### ADRs written this session:
- **`docs/adr/0011-worklog-promotion-ai-extraction-pipeline.md`** — Documents the deferred AI-assisted extraction pipeline: entity extraction → targeted Q&A → sanitization → skill mapping → résumé draft → human review. Photos NEVER sent to LLM. Current dialog is Option B baseline.
- **`docs/adr/0012-asset-library-knowledge-backbone.md`** — Full design for Asset Library as system connective tissue: `AssetType` (class layer) + `JobAsset` instances (inherits type knowledge) + `AssetDocument`, `AssetLink`, `AssetSkill`, `Procedure`, `ProcedureStep` models. 3-sprint sequence defined.

## "Live" Context

### Uncommitted files (need a commit before next session):
```
M  src/components/position-worklog-tab.tsx
M  src/components/worklog/worklog-note-reader.tsx
M  src/components/worklog/worklog-page.tsx
?? docs/adr/0011-worklog-promotion-ai-extraction-pipeline.md
?? docs/adr/0012-asset-library-knowledge-backbone.md
?? src/components/worklog/promote-to-event-dialog.tsx
```
**Commit message to use:**
```
feat(worklog): Phase D — promote notable entries to career events

- promote-to-event-dialog.tsx: new dialog (title, category, description, metrics)
- worklog-note-reader: Promote button + Promoted badge + dialog render
- position-worklog-tab: replace placeholder banner with live promote UI
- docs/adr/0011: AI extraction pipeline (deferred sprint)
- docs/adr/0012: Asset Library as knowledge backbone
```

### HEAD commit:
`83252ca` — feat(worklog): canvas block (tldraw v5), focus-restore, edit-mode fix, tags-prep

### Branch: `feature/interactive-resume-split-layout`
### Do NOT push unless user explicitly says "push"
### Use `git -c gc.auto=0 commit` to avoid OneDrive gc lock loops

## Next Immediate Step

**Resume prompt:**
> "Session Start. Commit the Phase D changes from last session, then let's work on [next task]."

**Likely next tasks (user is thinking on these, no decision made):**
1. **Tools & Assets UX redesign** — user doesn't like the stacked accordion dropdowns in the note reader. Three options were presented, none chosen yet:
   - Option 1: Icon buttons (🔧 ⚙) in the top action bar → modal dialog per type
   - Option 2: Chips merged into the metadata row (date · category · position · 🔧 chip · ⚙ chip)
   - Option 3: Accordions only visible when items are attached, `+ Attach` ghost link at bottom
2. **Asset Library Sprint 1** (ADR-0012) — `AssetType` model + documents + links schema migration + detail page UI
3. **AI Extraction Pipeline Sprint** (ADR-0011) — server-side LLM route + clarification Q&A in promote dialog

## Unresolved Blockers

- **Dev server not running** — `npm run dev` exits with code 1 in all terminals. The build error (Turbopack parse error on worklog-note-reader) was fixed in code but the server was never restarted to confirm. First thing in next session: start dev server and verify Phase D renders correctly.
- **Tags autocomplete** — shipped in `83252ca` but not visually confirmed in the running app this session.

## UI Graph Status

Three new tokens added to `.github/ui/tokens.md`:
- `state.promote-action` — amber interactive button (Promote CTA)
- `state.promote-surface` — amber card/banner surface for promotable-entry callouts
- `state.promoted` — emerald inline "Promoted" indicator

`worklog.md` updated with **Promotion State Patterns** section covering the Promote button, Promoted indicator, and banner card anatomy.
