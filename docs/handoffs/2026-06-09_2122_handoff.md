# Session Handoff — 2026-06-09 21:22

## Current Sprint

**ADR-0017 — Worklog version history + visual diff.** End-to-end version snapshots, browse-by-date timeline, plain-text diff against current document, and reversible restore.

## Last Completed Step

**ADR-0017 fully shipped + smoke-tested + dev-time bugs fixed + memory captured.**

- All 10 phases landed: schema → migration → pure helpers (TDD) → list / per-version / restore routes (TDD) → auto-snapshot writer in PUT route (TDD) → retention thinning (TDD) → `<WorklogHistoryPanel>` → `<WorklogVersionDiffModal>` → toolbar Save Version button.
- Test suite: **445/445 across 32 files in ~1.7s** (`npm test`).
- Commits on `origin/main`: `8d8150b` (helpers + routes), `2a88c9b` (UI panel + diff modal), `df4749d` (window.prompt/confirm → base-ui Dialog).
- Browser-verified: panel renders, Save Version dialog accepts label and saves snapshot ("smoke test v1" with `+38` charDelta), View Diff modal renders green/red segments, Restore dialog confirms and creates a `Before restore from <ts>` pre-snapshot then restores. Post-restore the panel shows `HISTORY (2)`.

## The "Live" Context

- Pure helpers: [src/lib/worklog/version/grouping.ts](src/lib/worklog/version/grouping.ts), [src/lib/worklog/version/diff.ts](src/lib/worklog/version/diff.ts).
- Routes: [src/app/api/work-logs/[id]/versions/route.ts](src/app/api/work-logs/%5Bid%5D/versions/route.ts), [src/app/api/work-logs/[id]/versions/[versionId]/route.ts](src/app/api/work-logs/%5Bid%5D/versions/%5BversionId%5D/route.ts), [src/app/api/work-logs/[id]/versions/[versionId]/restore/route.ts](src/app/api/work-logs/%5Bid%5D/versions/%5BversionId%5D/restore/route.ts).
- UI: [src/components/worklog/worklog-history-panel.tsx](src/components/worklog/worklog-history-panel.tsx), [src/components/worklog/worklog-version-diff-modal.tsx](src/components/worklog/worklog-version-diff-modal.tsx), mount in [src/components/worklog/worklog-note-reader.tsx](src/components/worklog/worklog-note-reader.tsx), Save-Version button in [src/components/worklog/worklog-editor-toolbar.tsx](src/components/worklog/worklog-editor-toolbar.tsx).
- Query keys: `["worklog-versions", noteId]`, `["worklog-version", noteId, versionId]`, `["work-logs"]`, `["worklog-backlinks", noteId]`.
- Asymmetric mount: History panel in **full-screen reader only**, NOT in the drawer reader. Backlinks remains in both.
- Tokenizer regex (diff): `/(\n)|([^\s\n]+)|([ \t]+)/g` — words, horizontal whitespace, newlines must stay as separate tokens.
- `groupVersionsByDate` `thisWeek` is anchored to NOW, not start-of-day.
- `WorklogEditorToolbar` `workLogId?: string` prop gates the Save Version button.

## Next Immediate Step

Pick one of:

1. **Tackle a parked item.** Either:
   - **Y.js restore broadcast** — POST `/api/work-logs/[id]/versions/[versionId]/restore` updates `contentJson` server-side but does not push a `Y.update` to other open editor sessions. Other devices see stale state until reload. Promote to a small ADR.
   - **Phase 9b — rich diff** — swap `computePlainTextDiff` for prosemirror-changeset-based block-level diff (lists, headings, mentions). Only justify if real users complain v1 is too coarse.
2. **Open the next ADR.** Check [docs/adr/](docs/adr/) for what's queued.
3. **Resume command suggestion**: `Session Start — 2026-06-?? HH:mm (local). Read latest handoff. Pick up <chosen task>.`

## Unresolved Blockers

- **PARKED — Y.js sync after restore.** Restore writes server state but does not broadcast `Y.update` to connected clients. Ship as-is; address only if multi-device editors hit it in practice.
- **PARKED — Phase 9b rich diff.** Plain-text LCS is shipped v1. Prosemirror-changeset is a future refactor of `computePlainTextDiff` only — UI stays the same.

## UI Graph Status

UI Graph Keeper audited [`.github/ui/worklog.md`](.github/ui/worklog.md), [`.github/ui/tokens.md`](.github/ui/tokens.md), and [`.github/ui/global.md`](.github/ui/global.md). One new section added to [worklog.md](.github/ui/worklog.md) — **"Version History Panel + Diff Modal (ADR-0017)"** — covering panel anatomy, version-row anatomy, restore confirm dialog (with the explicit `window.confirm` ban), diff-modal layout, and the Save-Version toolbar button. Last-updated header bumped to 2026-06-09. **No new tokens introduced** — `charDelta` colors and diff highlights stay inline raw Tailwind (one-off visualizations, not reusable surfaces); a `state.delta-*` / `state.diff-*` token would only be justified by a second consumer.

## Memory Graph Status

Memory Keeper opened nodes for `[WorklogDomain, WorklogReaderDrawer, WorklogNoteReadView, WorklogHistoryPanel, WorklogVersionDiffModal, WorklogEditor, WorklogEditorToolbar]` — 3 returned (`WorklogDomain`, `WorklogReaderDrawer`, `WorklogNoteReadView`), 4 absent. Writes:

- **`WorklogDomain`** — appended **9 new observations** covering: ADR-0017 ship, schema (`WorkLogVersion`), new routes (list / per-version / restore), pure helpers + their gotchas, asymmetric mount surface, `workLogId?` toolbar prop, `window.prompt`/`window.confirm` trap, parked Y.js restore broadcast, parked Phase 9b rich diff, and post-ship test count (445/445).
- **`WorklogHistoryPanel`** — newly created with **9 observations** (path, sibling-to-Backlinks role, mount surface, query/staleTime, bucket grouping, row anatomy, empty state, restore-dialog pattern, mutation invalidation set, diff-modal trigger ownership).
- **`WorklogVersionDiffModal`** — newly created with **6 observations** (path, prop shape with required `noteId`, per-version GET fetch, LCS tokenizer regex, segment styling, parked Phase 9b).
- **3 new relations**: `WorklogDomain → contains → WorklogHistoryPanel`, `WorklogDomain → contains → WorklogVersionDiffModal`, `WorklogHistoryPanel → embeds → WorklogVersionDiffModal`.
- Mirrored to disk shard [`docs/memory/worklog.json`](docs/memory/worklog.json) and [`docs/memory/index.json`](docs/memory/index.json) for rebuild safety.
