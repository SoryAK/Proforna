# Handoff — 2026-06-25 13:15 EDT

## Current Sprint

**ADR-0051 — Document Substrate Foundation** (Sprint α'). All three planned commits landed; substrate is live and validated end-to-end. ADR Status: Accepted (2026-06-25).

This unblocks **ADR-0050 (Equipment Manuals KB)** — the user-facing feature that will consume `createDocument` + `attachDocument` + the markitdown extraction pipeline (deferred to ADR-0050 per ADR-0051 §Implementation Notes).

## Last Completed Step

Committed `3440b86` — `chore(rules,memory): schema-faithful mocks rule 4 (Prisma variant) + DocumentDomain memory`. Wrapped up Phase 4 persistence: extended Schema-Faithful Mocks skill with checked/unchecked variant rule, created `DocumentDomain` memory entity + shard backup, logged the rule edit in `workflow-change-log.json`.

## Live Context

**Sprint α' commit chain (all on `main`, all unpushed per user policy):**

| SHA | Title | Δ Tests | Δ Coverage (src/lib) |
| --- | ----- | ------- | -------------------- |
| `a60ba06` | feat(documents): Document substrate fields + disk-backed GET (Commit 1) | 1017 → 1017 | (schema-only) |
| `f3b36df` | feat(documents): storage service + disk-backed uploads (Commit 2) | 1017 → 1028 (+11, +4 storage.test) | +0.33% |
| `b68f91d` | feat(asset-document): attachDocument + getAssetDocumentFile + upload routes (Commit 3) | 1028 → 1041 (+13) | +0.83% |
| `3440b86` | chore(rules,memory): wrap-up | 1041 (no change) | n/a |

**Live smoke validation (run during Commits 2 + 3):**

- Commit 2: `POST /api/documents` multipart → disk file written at `storage/documents/{userId}/{docId}.pdf`; `GET /api/documents/{id}` returns 52/52 bytes match
- Commit 3: `POST /api/asset-types/{id}/documents` multipart → AssetDocument row with `documentId` set, `filePath`/`url` null; `GET /api/asset-documents/{id}/file` returns 44/44 bytes match, `Content-Type: application/pdf`; URL-only AssetDocument GET returns 404 (clean degrade)

**Helper API now in `src/lib/documents/`:**

- `storage.ts` — `createDocument(input)`, `readDocumentBytes(doc)`, `DEFAULT_STORAGE_ROOT = {repo}/storage/documents`
- `asset-document.ts` — `attachDocument(input)` (XOR-enforced), `getAssetDocumentFile(adId)` (three-branch: documentId / legacy filePath under `public/uploads` / url-only throws), `VALID_DOC_TYPES` set, `LEGACY_FILE_ROOT`

**Critical Prisma pattern (now codified in rule 4 of Schema-Faithful Mocks):** for any helper that takes `{ id, userId, ... }` and writes via `prisma.<model>.create`, use the relation `connect` form unconditionally — `user: { connect: { id: input.userId } }`. The scalar `userId: input.userId` shape can trip the runtime variant validator into "Argument `user` is missing" even though TS accepts it. Mocks cannot catch this; only live integration does.

**Schema state:** migration `20260625151215_add_document_substrate_fields` applied; `prisma migrate status` clean. `Document.data` is nullable; `AssetDocument.documentId` FK active; `documents_user_id_processing_status_idx`, `documents_content_hash_idx`, `asset_documents_document_id_idx` indexes live.

**Files NOT YET migrated to substrate (out of Sprint α' scope):**

- `Attachment` model still uses `public/uploads/attachments/{filename}` (IDOR candidate; queued for **ADR-0052**)
- Legacy `Equipment` table still uses `EquipmentResource.fileData Bytes?` (intentional per ADR-0051 Q2; manuals KB will be JobAsset-only)
- Existing `Document.data`-backed rows kept indefinitely (revisit on contentHash-dedup work)

## Next Immediate Step

Two viable threads, user picks:

**Thread A — ADR-0050 (Equipment Manuals KB) implementation:** Now unblocked. Sketch sprint: (β1) markitdown extraction worker + processingStatus state machine, (β2) chunking + embedding pipeline, (β3) RAG endpoint with strict-citation contract, (β4) AssetType detail page UI surfacing manuals + ask-the-manual. Start by re-reading ADR-0050 and Griller-checking the worker boundary.

**Thread B — ADR-0052 (Attachment unification):** Cheap follow-up to ADR-0051. Add `Attachment.documentId String?` FK, write `attachAttachment` helper mirroring `attachDocument`, refactor the worklog attachment upload route to write through the substrate, ship behind the same lazy-migration discipline (legacy `Attachment.filePath` rows stay readable). Closes the remaining IDOR vector.

Suggested wording to resume: *"resume — pick thread A (ADR-0050 β1 worker) or thread B (ADR-0052 Attachment substrate). Start with ADR re-read + Griller."*

## Unresolved Blockers

- **Four commits unpushed** by user policy: `a60ba06`, `f3b36df`, `b68f91d`, `3440b86`. Push when ready with `git -c gc.auto=0 push origin main` (or `git -c gc.auto=0 push origin HEAD:main`).
- **SHA backfill pending:** the `testing:schema-faithful-mocks-variant-selection` entry in `workflow-change-log.json` was updated post-commit to set `commit: "3440b86"` — that one-line change is uncommitted in the working tree and should ride along with the next commit (or be committed standalone as `chore(workflow-log): backfill commit SHA`).

## UI Graph Status

UI Graph Keeper did **NOT** run — no `.tsx` files were modified this session. All Sprint α' work was in `src/lib/documents/`, `src/app/api/`, `prisma/`, and persistence-layer files.

## Memory Graph Status

Memory Keeper audited the session and made two writes:

- **`PrismaSchemaOps`** (existing entity) — appended 2 observations dated 2026-06-25: (a) checked/unchecked input variant runtime quirk + connect-form fix; (b) EPERM regen recovery loop on OneDrive-synced Windows with stale-engine detection recipe.
- **`DocumentDomain`** (NEW entity, entityType=`domain`) — created with 6 observations covering: ADR-0051 origin + commit chain, storage architecture (private `<repo>/storage/`), public API surface in `src/lib/documents/`, route surface, schema fields added by migration `20260625151215`, known gotchas (XOR helper-only enforcement, Equipment exclusion, kept `data` column, Attachment IDOR follow-up).

Shard backups synced: `docs/memory/schema-ops.json` updated, `docs/memory/documents.json` created, `docs/memory/index.json` registered `DocumentDomain: documents.json` and bumped `last_updated` to 2026-06-25. All landed in commit `3440b86`.

## Workflow Review Cadence

✅ **on cadence — 5 days since last review.** Last review: 2026-06-19 (Tier B sprint). This session did NOT perform a workflow review — pure feature work for ADR-0051 — so no append to `workflow-reviews.json`. Next review window opens at 7 days (≈ 2026-06-26).

## User Practice Adoptions

Probed. User confirmed adoption of one new **agent rule** (not a user-side practice): `testing:schema-faithful-mocks-variant-selection` — Schema-Faithful Mocks skill rule 4 (Prisma checked/unchecked variant → use `connect` form). Logged to `workflow-change-log.json` `agentRuleEdits` stream with commit `3440b86`. No new user-side behavioral practices or infrastructure/tooling changes this session.

---

**Composite gate satisfied:** Memory Keeper ran (PrismaSchemaOps `add_observations` + DocumentDomain `create_entities` visible above); Practice Adoption probe ran (user answered "yes", logged the rule, recorded above). Handoff written.
