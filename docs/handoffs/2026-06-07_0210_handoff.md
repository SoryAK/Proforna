# Session Handoff — 2026-06-07 02:10

## Current Sprint

ADR-0015 — `/worklog/notes` document-manager polish. The redesigned 2-pane (list + reader) document UI passed a 6-item self-audit; only the reader-as-route concern (improvement #5) remains, deferred to ADR-0015 Phase 5–6.

## Last Completed Step

Closed every item in the self-audit list except #5 (which was explicitly deferred), all shipped to `origin/main`.

- Commits this session: `5ebfcae` (#2 shared notes module), `470c111` (#1 sort dropdown), `0fdbc69` (#4 grid select-all + bulk view-control passthrough), `7234331` (#3 ARIA radiogroup keyboard nav), `76187b5` (#6 storage-key namespace + central registry).
- Pushed range: `bb5d9eb..76187b5` on `main`.
- New file: `src/lib/storage-keys.ts` — `STORAGE_KEYS` registry + `migrateLegacyKey()` lazy migrator.
- Migrated callers: `src/components/sidebar.tsx`, `src/components/worklog/worklog-activity-widget.tsx`, `src/components/worklog/worklog-folders-rail.tsx`, `src/components/worklog/worklog-folder-tree.tsx`, `src/components/documents/types.ts`, `src/components/documents/use-documents-view.ts`.
- Audit feature changes (still in `src/components/worklog/worklog-notes-view.tsx` + sibling files): view-mode radio group has roving `tabIndex`, Arrow/Home/End keyboard nav with wrap, focus-visible ring; sort menu present on grid; bulk bar accepts `trailing?: ReactNode` so view-mode + sort stay visible during bulk; shared helpers live in `src/components/worklog/worklog-notes-shared.ts`.
- Observable outcome: `/worklog/notes` view-mode toggle is keyboard-operable, sort works in both list and grid, bulk-select respects "select all in current filter", and every namespaced localStorage key migrates legacy values on first read.

## The "Live" Context

- View-mode radiogroup keyboard nav lives at `src/components/worklog/worklog-notes-view.tsx` — search for `handleViewModeKeyDown`. Order is `["list","grid"]`; focus follows selection via `root.querySelector('[data-view-mode="${next}"]')?.focus()`.
- `STORAGE_KEYS` shape (single source of truth at `src/lib/storage-keys.ts`):
  - `sidebar.collapsed` → `resumsify:sidebar:collapsed`
  - `sidebar.worklogShowGlobal` → `resumsify:sidebar:worklog-show-global`
  - `worklog.railActivityOpen` → `resumsify:worklog:rail-activity-open`
  - `worklog.railCategoriesOpen` → `resumsify:worklog:rail-categories-open`
  - `worklog.folderTreeExpanded` → `resumsify:worklog:folder-tree-expanded`
  - `documents.viewMode` → `resumsify:documents:view-mode`
- `migrateLegacyKey()` is idempotent and silent on quota errors — call it from the same `useEffect` that performs the first read.
- Out-of-scope (intentionally not migrated this pass): `resumsify-ai-*` (already prefixed), `gallery-settings` (gallery-modal local state). Folded into a parked-ideas note for the next storage audit.
- Recently-rejected approach: a refactor of the bulk bar that hid sort/view controls during bulk — replaced with the `trailing` slot pattern instead.

## Next Immediate Step

> "Pick up ADR-0015 Phase 5–6 — the reader-as-route redesign for `/worklog/notes`. Goal: when a note is selected, push it to a sub-route (e.g. `/worklog/notes/<id>`) so list context survives reload/back-nav. This also resolves audit improvement #5 (reader currently loses list filters on refresh)."

## Unresolved Blockers

- [ ] **MCP memory supervisor scheduled task not installed** — supervisor must be relaunched manually after each reboot. Manual launch sequence captured in user memory under "MCP Memory Supervisor Manual Launch". Today's PID was `12080`; ports `3717/3718/3719` verified Listen at 06:06:19 UTC.
- [ ] **Workflow Logger discovery** — the canonical wrapper script `$env:LOCALAPPDATA\mcp-memory\start-memory-server.ps1` only mentions 2 of 3 supervisor ports in its comment header (stale doc). Worth fixing the next time the wrapper is touched.
- [ ] **Improvement #5 deferred** — reader-pane loses list context on hard reload (covered in Next Immediate Step).

## Optional: Deferred Ideas

- Storage-key audit pass 2: fold `resumsify-ai-*` and `gallery-settings` into `STORAGE_KEYS` for full uniformity.
- Reader-pane redesign may need a route-aware `useFolderSelection` adjustment so deep links to `/worklog/notes/<id>?folder=...` round-trip.

## UI Graph Status

UI Graph Keeper audited `src/components/worklog/worklog-notes-view.tsx`, `src/components/worklog/worklog-notes-bulk-bar.tsx`, `src/components/worklog/worklog-notes-shared.ts` — no new tokens or patterns introduced. Keyboard nav reuses the existing ARIA radiogroup pattern; focus-visible ring uses the standard `ring-ring` token; the bulk bar `trailing` slot is a local prop, not a global pattern.

## Memory Graph Status

Memory Keeper added 1 new entity and 7 new observations:

- **`StorageKeysModule`** (new entity, type `module`) — 4 observations on the `resumsify:<feature>:<key>` convention, the `STORAGE_KEYS` registry, the `migrateLegacyKey` ledger, and the out-of-scope items.
- **`LayoutShellDomain`** — 1 observation on the sidebar key rename.
- **`WorklogDomain`** — 2 observations on rail/folder key migration + ADR-0015 audit closeout.
- New relations: `LayoutShellDomain depends-on StorageKeysModule`, `WorklogDomain depends-on StorageKeysModule`.
- Mirrored to shards: `docs/memory/storage-keys.json` (new), `docs/memory/auth-layout.json` (updated), `docs/memory/worklog.json` (updated). `docs/memory/index.json` registers the new shard and bumps `last_updated` to `2026-06-07`.
- User memory updated: added "MCP Memory Supervisor Manual Launch" section to `/memories/resumsify-lessons.md` capturing the supervisor.js argument requirement and the `Get-NetTCPConnection` verification pattern.
