# Handoff — ADR-0029 P0 Audit Sprint Complete

**Date:** 2026-06-15 22:33 (local)
**Branch:** `main` (3 commits ahead of any push — Git Push Policy still locked)

---

## Current Sprint

ADR-0029 (worklog procedures) post-acceptance audit. Three P0 issues caught after the initial Phase 4 wrap. The user locked the order at session start: **#3 → #2 → #1**, one atomic E→T→C commit per unit, no batching.

## Last Completed Step

**All 3 P0 units shipped in a single dev session.** Tests 687 → 697 (+10). Zero scope creep, zero `linkedNoteIds` references remaining anywhere in code.

| Unit | Commit | Title | Tests |
|---|---|---|---|
| A (P0-#3) | `8c57b3b` | `fix(worklog): POST /api/work-logs 400 on unknown kind (ADR-0029 P0-#3)` | +1 |
| B (P0-#2) | `d517676` | `fix(worklog): search route default-filters to kind='note' (ADR-0029 P0-#2)` | +3 |
| C (P0-#1) | `764850a` | `feat(worklog): rename linkedNoteIds -> linkedWorkLogIds, project @r: too (ADR-0029 P0-#1)` | +6 |

**Unit C was the structurally significant one.** Schema migration `20260615222114_rename_linked_note_ids_to_linked_worklog_ids` applied via the locked migration-drift recipe (`prisma db execute --stdin` → `prisma migrate resolve --applied` → `prisma generate`). Backfill script written + dry-run validated (11 rows scanned, 6 with contentJson, 0 updates needed — kind-just-shipped baseline).

## Live Context

- **Vitest baseline:** `697/697` green (`npm test 2>&1 | Select-Object -Last 12`).
- **Dev server:** restarted after the Unit C migration (terminal `2bc7b7d3-a1ba-4bf5-b2e1-c6043dcaa850`). Browser smoke not yet performed.
- **DB state:** dev Postgres has the renamed column + renamed GIN index. No procedures exist yet with `@r:` chips inside their content (so backfill found nothing to backfill — by design).
- **Files now warm in memory:**
  - [src/app/api/work-logs/route.ts](src/app/api/work-logs/route.ts#L145) — explicit kind whitelist gate.
  - [src/app/api/work-logs/search/route.ts](src/app/api/work-logs/search/route.ts) — `kindPredicate` composed into ranked CTE.
  - [src/app/api/work-logs/[id]/route.ts](src/app/api/work-logs/%5Bid%5D/route.ts) — PUT widens to union of `@n:` + `@r:` extraction.
  - [src/app/api/work-logs/[id]/backlinks/route.ts](src/app/api/work-logs/%5Bid%5D/backlinks/route.ts) — kind-agnostic by name and behavior.
  - [src/app/api/work-logs/import-md/route.ts](src/app/api/work-logs/import-md/route.ts) — same union widening.
  - [prisma/schema.prisma](prisma/schema.prisma#L760) — `linkedWorkLogIds` field + GIN index.
  - [scripts/migrations/2026-06-15-backfill-linked-worklog-ids.ts](scripts/migrations/2026-06-15-backfill-linked-worklog-ids.ts) — idempotent backfill, mirror of PUT.

## Next Immediate Step

> Smoke-test the three units in the browser, then proceed to the next sprint item. Recommended smokes:
> 1. POST `{kind:"banana"}` to `/api/work-logs` → 400 with `error: "kind must be 'note' or 'procedure'"`.
> 2. Search box on `/worklog/notes` returns only notes; search box on `/worklog/procedures` returns only procedures.
> 3. Create a note with `@r:<some-procedure>` chip → save → visit that procedure → confirm the note appears in the backlinks panel (this is the structural win — would not have worked before P0-#1).

## Unresolved Blockers

- **MCP memory 503** ("Single-client backing server already occupied") prevented the live `mcp_memory-resums_add_observations` write this session. **Fix is VS Code-side only**: Command Palette → "MCP: Restart Server" → pick `memory-resumsify`. Shard JSON in [docs/memory/worklog.json](docs/memory/worklog.json) was updated directly (3 new dated observations); next session's Memory Keeper should re-sync to the live graph after the MCP restart, OR rely on the shard rebuild protocol.

## Memory Graph Status

Memory Keeper attempted `mcp_memory-resums_open_nodes(["WorklogDomain"])` and hit the known 503 lockout. Fell back to the shard backup protocol — appended 3 new dated observations directly to [docs/memory/worklog.json](docs/memory/worklog.json) (`WorklogDomain.observations.length` 77 → 80). No new entities or relations warranted — all changes are observations on the existing `WorklogDomain` aggregate.

## UI Graph Status

UI Graph Keeper not invoked this session — no `.tsx` files were touched. All edits were API routes, schema, migration, and tests. The user's Phase 1.5 directive (UI graph) does not apply.

## Files Modified This Session

```
prisma/schema.prisma                                               (Unit C)
prisma/migrations/20260615222114_rename_linked_note_ids_to_linked_worklog_ids/migration.sql  (NEW, Unit C)
scripts/migrations/2026-06-15-backfill-linked-worklog-ids.ts       (NEW, Unit C)
src/app/api/work-logs/route.ts                                     (Unit A)
src/app/api/work-logs/route.test.ts                                (Unit A)
src/app/api/work-logs/search/route.ts                              (Unit B)
src/app/api/work-logs/search/route.test.ts                         (Unit B)
src/app/api/work-logs/[id]/route.ts                                (Unit C)
src/app/api/work-logs/[id]/route.test.ts                           (Unit C)
src/app/api/work-logs/[id]/backlinks/route.ts                      (Unit C)
src/app/api/work-logs/[id]/backlinks/route.test.ts                 (Unit C)
src/app/api/work-logs/import-md/route.ts                           (Unit C)
src/app/api/work-logs/import-md/route.test.ts                      (Unit C)
src/lib/array-set-equal.ts                                         (Unit C, doc rename only)
src/lib/array-set-equal.test.ts                                    (Unit C, doc rename only)
docs/adr/0029-worklog-procedures.md                                (Phase 4 — P0 follow-up section)
docs/memory/worklog.json                                           (Phase 4 — 3 new observations)
```
