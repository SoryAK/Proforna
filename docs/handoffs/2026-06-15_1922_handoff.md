# Handoff — 2026-06-15 19:22 (local)

## Current Sprint

Closing parked-ideas.md gap #1 (persona/contact reverse-lookup endpoint) and partially gap #3 (discoverability) via ADR-0028. App-wide CRM-lite knowledge-graph polish.

## Last Completed Step

**ADR-0028 SHIPPED end-to-end**, commit `3b413de`. The `@p:` mention chip is no longer write-only — opening any contact in the `/contacts` edit Dialog now surfaces "Notes mentioning this contact (N)" with the discoverability hint as the empty state. Live-verified with seeded smoke fixtures (`smoke-contact-adr0028` + `smoke-worklog-adr0028`); rows clicked through to `/worklog/notes?focus=<id>`. Smoke records cleaned up.

5-unit ship in one cohesive commit:

| Unit | Surface | Outcome |
|---|---|---|
| 1 | `prisma/schema.prisma` + migration `20260615170725_add_worklog_linked_contact_ids` + backfill | `WorkLog.linkedContactIds: String[]` GIN-indexed; backfill verified 6 rows / 0 updates (clean baseline) |
| 2 | `src/app/api/work-logs/[id]/route.ts` (PUT) | Derives `linkedContactIds` from contentJson on save; REPLACE semantic; skip-if-equal guard; 5 new vitest cases |
| 3 | `src/app/api/contacts/[id]/backlinks/route.ts` (GET) | Owner-scoped twice (404 cross-user, NOT empty array); 5 vitest cases |
| 4 | `src/components/contacts/contact-backlinks-section.tsx` mounted in `/contacts` edit Dialog | TanStack Query useQuery; click-row → `/worklog/notes?focus=<id>`; @p: discoverability hint in empty state |
| 5 | Phase 4 wrap | ADR-0028 → Accepted, Manual entry, Workflow rev 3, parked-ideas update, Memory Keeper, this handoff |

Test suite **673/673** PASS (663 baseline + 10 new = 5 PUT + 5 backlinks). Browser smoke confirmed.

## The "Live" Context

- **ADR-0028** — accepted 2026-06-15. Out-of-scope clauses with re-entry triggers spelled out.
- **`docs/workflows/wire-mention-entity-type.md`** — revved to v3 with the cross-cut backlinks variant codified (no `NOT: { id }`, add owner-entity findFirst gate, route is `/api/<owning-entity>/[id]/backlinks`).
- **`.Manual/contact-backlinks.md`** — created with full functional + internal-workflow + constraints + future sections.
- **MCP Memory** — `ContactDomain` entity created (read-then-write protocol followed: `open_nodes` returned only `WorklogDomain`); `WorklogDomain` got one observation appended. Shard backups: new `docs/memory/contacts.json` + `docs/memory/index.json` map updated.
- **parked-ideas.md** — gap #1 marked ✅ RESOLVED via ADR-0028, gap #3 marked ⚠️ PARTIAL (toolbar hint / slash-command / contact-page CTA still parked); gaps #2, #4, #5 unchanged.

**Files committed in `3b413de`** (12 files, +908 -3):
- `docs/adr/0028-persona-contact-reverse-lookup.md` (Status: Accepted)
- `prisma/schema.prisma` (linkedContactIds field + GIN index)
- `prisma/migrations/20260615170725_add_worklog_linked_contact_ids/migration.sql`
- `scripts/migrations/2026-06-15-backfill-linked-contact-ids.ts`
- `scripts/migrations/2026-06-15-smoke-seed-adr0028.ts`
- `scripts/migrations/2026-06-15-smoke-cleanup-adr0028.ts`
- `src/app/api/work-logs/[id]/route.ts` (write path)
- `src/app/api/work-logs/[id]/route.test.ts` (+5 cases, 33/33 in file)
- `src/app/api/contacts/[id]/backlinks/route.ts` (read path)
- `src/app/api/contacts/[id]/backlinks/route.test.ts` (+5 cases)
- `src/components/contacts/contact-backlinks-section.tsx`
- `src/app/(app)/contacts/page.tsx` (mount in edit Dialog)

**Files modified post-commit (Phase 4 docs, NOT yet committed):**
- `.Manual/contact-backlinks.md` (new)
- `docs/workflows/wire-mention-entity-type.md` (rev 3)
- `docs/memory/contacts.json` (new)
- `docs/memory/index.json` (ContactDomain entry)
- `docs/memory/worklog.json` (WorklogDomain observation appended)
- `docs/handoffs/2026-06-15_1922_handoff.md` (this file)

These six files want a follow-up `docs(memory):` commit at next session start.

## Next Immediate Step

Pick one of:

1. **(Recommended)** Stage + commit Phase 4 docs as a single `docs(memory): ADR-0028 wrap — Manual + workflow rev3 + memory shards + handoff` commit. Heredoc → `Out-File .git/COMMIT_MSG_TMP` → `git -c gc.auto=0 commit -F .git/COMMIT_MSG_TMP`. Do NOT push.
2. **(Alternative sprint)** Pick the next parked-ideas item — top candidate is gap #4 (cross-cut contact mentions inside JobApplication / Interview / RecruiterSubmission), which would re-use the `wire-mention-entity-type.md` rev 3 cross-cut recipe directly.
3. **(Tooling)** The dev server (`npm run dev`) was still running on port 3000 at the end of session under terminal id `e2914e37-6d45-442d-b2d9-507fa9775d60`. Kill it before next session if not already terminated by VS Code shutdown.

## Unresolved Blockers

None. ADR-0028 is fully shipped, smoke-verified, and persisted.

Soft watchpoints (not blockers):
- Phase 4 docs are not yet committed — see Next Immediate Step #1.
- ADR-0016's worklog backlinks route still empty-arrays cross-user worklog ids (looser owner scope than ADR-0028's contact backlinks). Parked under ADR-0028 Out-of-Scope §5.

## UI Graph Status

UI Graph Keeper audited `src/components/contacts/contact-backlinks-section.tsx` and `src/app/(app)/contacts/page.tsx` — no new tokens or patterns introduced. The new component reuses existing tokens (`text-xs font-semibold uppercase tracking-wide text-muted-foreground` for the section header; `border rounded-md` row chrome with `hover:bg-muted/50`; `text-sm` body / `text-xs text-muted-foreground` meta — all canonical patterns). Empty-state hint copy follows the existing rail-empty pattern from `WorklogBacklinksPanel`. No new colors, no new spacing scale, no new motion tokens.

## Memory Graph Status

Memory Keeper audited session and gated writes via `mcp_memory-resums_open_nodes(["ContactDomain", "WorklogDomain"])` — confirmed `WorklogDomain` present, `ContactDomain` absent. Wrote:

- **`ContactDomain`** (NEW entity, type=domain) — 8 observations baked in via `mcp_memory-resums_create_entities` covering ADR-0028 schema, write path, read path, owner-scope policy, UI surface, test coverage, recipe re-validation, and out-of-scope clauses. Verified response contains the entity name.
- **`WorklogDomain`** (existing entity) — 1 observation appended via `mcp_memory-resums_add_observations` recording the `linkedContactIds` addition as the 5th array projection and noting that the PUT route now derives 3 array columns at save time (assetIds additive; linkedNoteIds replacement+self-loop; linkedContactIds replacement no self-loop).
- **Shard backup** — created `docs/memory/contacts.json` with the same observations, registered in `docs/memory/index.json` shards map; appended the WorklogDomain observation to `docs/memory/worklog.json`.

No relations created (Contact↔WorkLog topology is sufficiently captured in the observation prose; not promoting to a graph edge until a second cross-cut consumer exists — rule of three).
