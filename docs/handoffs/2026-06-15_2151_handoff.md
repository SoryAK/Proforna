# Session Handoff — 2026-06-15 21:51

## Current Sprint

ADR-0029 — Worklog Procedures (Runbooks). Add a `kind` discriminator to `WorkLog` and ship a procedures surface that reuses the notes editor + reader + version history + folders + DnD + search infrastructure. Sixth mention prefix `@r:` for runbooks; cross-cut "procedures linked to this asset" panel inside `AssetDetailModal`.

## Last Completed Step

ADR-0029 SHIPPED end-to-end. All 8 units green, 687/687 tests, zero in-flight work. Phase 4 wrap complete (ADR Accepted, Manual written, recipe rev 4 codified, parked-ideas captured, memory + shard updated).

- Commits (chronological):
  - `7a69ff6` — Unit 1 schema + migration (`WorkLog.kind` + composite index)
  - `48bcef1` — Unit 2 GET kind filter (+2 tests)
  - `dcae948` — Unit 3 PUT kind audit guard (+1 regression test)
  - `7e9cf54` — Units 4+5+6 procedures page + sidebar row + POST kind acceptance (+3 tests)
  - `7b415b6` — Unit 7 `@r:` mention type + kind-discriminator filter on existing `worklog` cases (+3 tests)
  - `0a01aee` — Unit 8 cross-cut `/api/job-assets/[id]/procedures` + `JobAssetProceduresSection` (+5 tests)
- Files touched (Phase 4 wrap, **uncommitted** at handoff time):
  - [docs/adr/0029-worklog-procedures.md](docs/adr/0029-worklog-procedures.md) — Status: Proposed → Accepted
  - [docs/workflows/wire-mention-entity-type.md](docs/workflows/wire-mention-entity-type.md) — rev 4 (sixth letter `r`, kind-discriminator filter as first-class step 6 + cross-cut composition note in step 8)
  - [.Manual/worklog-procedures.md](.Manual/worklog-procedures.md) — new
  - `/memories/repo/parked-ideas.md` — 9-item Worklog Procedures backlog at top
  - `docs/memory/worklog.json` — 4 new observations on `WorklogDomain` (mirrors MCP write)
- Observable outcome: User can navigate to `/worklog/procedures`, click "+ New procedure" to create one, see the new sidebar row with a count badge, type `@r:` in any worklog note to mention a procedure, and expand the new collapsible "Procedures" section in any asset's edit dialog to see runbooks that reference it.

## The "Live" Context

- **Test baseline**: 687/687 passing (vitest), 54 test files. Was 673/673 at session start.
- **Recipe rev 4** is the current canonical wire-mention-entity-type spec — adds the kind-discriminator filter step (when the new type shares a Prisma model with an existing type via a discriminator column) and codifies cross-cut composition (existing GIN column + new kind filter).
- **Critical pattern shipped**: `/api/job-assets/[id]/procedures` reuses the existing `assetIds: String[]` GIN column AND adds `kind: 'procedure'` — no new column, no new index, no new migration. This is the recipe-rev-4 step 8 cross-cut composition.
- **Migration drift recipe used**: `prisma migrate dev` rejected because ADR-0028's migration was edited post-apply. Manual SQL → `prisma db execute --stdin` → `prisma migrate resolve --applied "20260615205242_add_worklog_kind"` cleared status. Already in user memory.
- **Active patterns referenced**: ADR-0027 single-table-inheritance precedent, ADR-0028 cross-cut backlinks recipe, ADR-0017 version history reuse, ADR-0016 mention pattern.
- **Recently-rejected approaches**:
  - Separate `Procedure` model (rejected — would fork editor, version history, folders, DnD, search; precedent from ADR-0027 favors discriminator).
  - Adding `kind` to PUT route's update whitelist (rejected — kind is set at create time only; flipping requires explicit endpoint per parked-idea #1).
  - Routing `/worklog/procedures/[id]` to a separate reader (rejected — procedures share the notes editor at `/worklog/notes/[id]` per recipe).
- **Dev server**: stopped during Unit 1 to free Prisma engine DLL, never restarted. UI smoke test deferred (see Next Immediate Step).

## Next Immediate Step

Phase 4 wrap files are uncommitted. Two ways to land:

**Option A (recommended) — single Phase 4 commit**:

> "Commit the ADR-0029 Phase 4 wrap (ADR Accepted, Manual, workflow rev 4, parked-ideas, memory shard)."

**Option B — defer commit, smoke test first**:

> "Start the dev server and smoke-test ADR-0029 end-to-end: create a procedure, see it in the sidebar count, mention it from a note via `@r:`, link an asset to it via `@a:`, then expand the asset's Procedures section and verify the back-pivot. Then we'll commit Phase 4."

Per Verification-First Bias (user-memory note 2026-06-15), **Option B is the right default** — but the agent stopped here because the lab loop (dev server + browser) is user-driven.

## Unresolved Blockers

- [ ] **UI smoke test deferred** — no observable runtime verification that `/worklog/procedures` renders, `+ New procedure` actually creates and routes, sidebar count refreshes, `@r:` picker appears mid-typing, `JobAssetProceduresSection` collapses/expands and queries on demand. All paths are unit-tested but not browser-tested. Run dev server + walk the 5-point smoke list at the bottom of `.Manual/worklog-procedures.md`.
- [ ] **Phase 4 wrap uncommitted** — 5 doc/memory files staged in working tree but not yet in a commit (deliberate — agent sequenced E→T→C per code unit but doc commit is a single atomic step).

## UI Graph Status

UI Graph Keeper did not run this session — the only UI changes were:

- New page `/worklog/procedures` (server shell + new client view component) — reuses existing tokens (Skeleton, Button, ClipboardList icon, year-grouped pattern).
- New sidebar row in `WorklogNavSidebar` — copy-paste of the Events row shape (no new tokens).
- New `JobAssetProceduresSection` component — uses existing `Badge` + `ChevronDown/Right` + `ClipboardList` + rose color stops already in `mention-node.ts` ENTITY_TYPE_CONFIG.
No new tokens, color stops, or layout patterns introduced. Existing `bg-rose-100/text-rose-800/dark:bg-rose-900/40/dark:text-rose-300` quartet is now the canonical "procedure" palette and is used in two places (mention chip + asset section row badge) — flag for `/.github/ui/` codification on next UI Graph Keeper sweep.

## Memory Graph Status

`open_nodes(["WorklogDomain", "WorklogProcedures", "MentionSystem", "WorklogReaderDrawer"])` returned 2 of 4 (WorklogDomain + WorklogReaderDrawer present; WorklogProcedures + MentionSystem absent — confirmed via the on-disk `docs/memory/worklog.json` shard not having those keys).

Decision per shallow-graph rule: **add observations to WorklogDomain only**, do NOT create a `WorklogProcedures` entity yet. Procedures are not architecturally distinct from notes (same model, same editor, same reader, same version history) — they're a kind discriminator. Creating a sibling entity would inflate the graph without surfacing new architectural insight. Re-entry trigger: if a future ADR adds procedure-specific UI primitives or a separate model, then promote to a dedicated entity.

`add_observations` wrote 4 dated entries on `WorklogDomain` covering: (1) ADR-0029 ship summary with all 8 commit hashes, (2) procedures share the editor + sidebar count + PUT preservation guard, (3) `@r:` is the sixth mention prefix and the kind-discriminator filter is a first-class step in recipe rev 4, (4) cross-cut composition pattern for `/api/job-assets/[id]/procedures`. Same 4 observations mirrored to `docs/memory/worklog.json` via direct shard write. Total observations on `WorklogDomain` now 77.
