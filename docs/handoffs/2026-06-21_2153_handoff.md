# Handoff — 2026-06-21 21:53 — ADR-0045 sprint complete

## 1. Current Sprint

**ADR-0045 (AI Provenance UI + Chat Mode Picker)** — 5-day sprint built on top of ADR-0044's `ai.generate` router. Goal: surface provider/model/usage to the user across all 14 AI routes, and expose the router's task-class vocabulary as a sticky-per-thread mode picker in the chat panel.

## 2. Last Completed Step

Day 5 wrap — ADR-0045 Status flipped from `Proposed` to `Accepted (2026-06-21)` with full Acceptance section and sprint commit table written. Memory Keeper applied 8 new observations to `AIServicesLayer` in both the MCP graph and the `docs/memory/ai-services.json` shard backup. Practice Adoptions probe answered (no new adoptions). About to commit Day 5 + push the full sprint to `origin/main` at user instruction.

## 3. The "Live" Context

- **Sprint files warm:** `docs/adr/0045-ai-provenance-and-mode-picker-ui.md` (Acceptance section L186-L213), `docs/memory/ai-services.json` (8 new observations after L18), `src/lib/ai/envelope.ts`, `src/components/ai-provenance-chip.tsx`, `src/components/ai-chat.tsx` (mode picker L470-L535, send wiring L185-L210), `src/app/api/ai/chat/route.ts` (task forwarding L27-L65), `src/app/api/ai/chat/route.test.ts` (7 hermetic cases).
- **Test baseline:** 880 passed | 5 skipped (was 873 at sprint start — +7 chat-route task forwarding cases; envelope's 13 tests landed in Day 1 commit `5c6758f`).
- **Open todo:** push to `origin/main`. Local has 7 unpushed commits ahead of remote (94fb9e1 → 5c6758f → 21c422e → 90d615c → 7590d6d → 105fd03 → Day 5 commit).

## 4. Next Immediate Step

In a future session, the natural follow-up work surfaced by ADR-0045 acceptance:

> *"Run the live chat-mode smoke for all 4 modes (`task=chat`, `ground`, `reason`, `summarize`) with Ollama warm and `RUN_LIVE_SMOKE_AI=1`, then do a single browser walkthrough of all 14 AI consumer surfaces to visually confirm the provenance chip renders. These are the two Day 4 acceptance criteria explicitly tagged as deferred follow-ups in the ADR's Acceptance section."*

## 5. Unresolved Blockers

None. Sprint shipped with one deliberate deferral:

- **Live chat-mode smoke + browser walkthrough** (deferred from Day 4) — gated on real network + Ollama warm. Hermetic coverage at the route-handler level (Day 4 commit `105fd03`) plus the existing ADR-0044 Day 5 live smoke covers the router contract. Visual chip render is one-prop pass-through under TypeScript so risk is low; tagged in ADR-0045 §Deviations as a quality-of-life check rather than a correctness gate.
- **`useAIQuery` / `useAIMutation` hook adoption** — hooks ship in `src/lib/hooks/use-ai-query.ts` but every Day 2 consumer kept its bespoke `mutationFn` (rate-limit retry, multipart, shared chains). Hooks are available for new consumers; existing 14 consumers unwrap envelopes inline. Not a blocker — flagged in ADR Acceptance as a potential future cleanup pass.

## 6. UI Graph Status

UI Graph Keeper did NOT run this session. Rationale: the sprint added a single shared component (`AIProvenanceChip`, already wired) and a small mode-picker pattern inside `ai-chat.tsx`. No new tokens, no new layout primitives, no recurring pattern surfaced across multiple slices. The chip placement convention is captured in the Memory Keeper observations instead — that is the more durable home for it.

## 7. Memory Graph Status

Memory Keeper appended 8 new observations to `AIServicesLayer` (verified via `mcp_memory-resums_open_nodes(["AIServicesLayer"])` pre-write and `mcp_memory-resums_add_observations` returning the full list). Subjects covered: ADR-0045 acceptance summary, envelope contract + helpers, conditional envelope pattern for cache-hit routes, full 14-route inventory by commit, chip placement convention, hook bypass rationale, chat mode picker contract, sprint commit table. Shard backup written to `docs/memory/ai-services.json` in the same edit so a MCP wipe is recoverable.

## 8. Workflow Review Cadence

Workflow Review Cadence: ✅ on cadence — 2 days since last review (last `reviewedAt` = 2026-06-19T18:00:00Z).

## 9. User Practice Adoptions

User Practice Adoptions: probed — no new adoptions this session. (User reply: *"no/nothing new"*.)
