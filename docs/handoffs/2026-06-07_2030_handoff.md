# Session Handoff — 2026-06-07 20:30

## Current Sprint

Hardening the agent customization stack. Two adjacent goals: (1) close out MCP memory server pain points (search algorithm, write-protocol race conditions, naming collisions) and (2) close the largest gap surfaced by the obra/superpowers comparison — no TDD discipline.

## Last Completed Step

**MCP memory infrastructure hardened.** Two patches applied to `@modelcontextprotocol/server-memory@0.6.3` upstream package, both idempotent and re-appliable via `npm run memory:patch`:

1. **Naming patch** — `McpServer` reads `process.env.MCP_SERVER_NAME` so the three supergateway children expose distinct tool names (`mcp_memory-resumsify_*`, `mcp_memory-global_*`, `mcp_memory-user-profile_*`). User-side window reload still pending to refresh the tool registry.
2. **Search tokenize patch** — `searchNodes` now AND-matches whitespace-split tokens across `name | type | observations`. Smoke-tested: `worklog drawer reader` returns 3 entities instead of 0.

**Memory Keeper write protocol rewritten** in [.github/instructions/persistence.instructions.md](.github/instructions/persistence.instructions.md) to gate every write with `mcp_memory_open_nodes` first. Three forbidden write patterns documented: blind `create_entities` for existence checks, `add_observations` without prior `open_nodes`, and treating `create_entities` response shape as a presence signal.

**TDD added as Phase 2.5 of the Execution Pipeline.** New instruction file with 5 skills (Iron Law, RED-GREEN-REFACTOR with mandatory verify steps, Bug-Fix Test-First, Red Flags & Rationalizations table with 10 entries, Verification Checklist). Scoped to logic dirs only — UI stays on the manual test sheet. ADR 0018 records the decision (1A logic-only + 2C bug-fix retroactive + 3A new Phase 2.5 placement).

- Commits: `d069002` (TDD skill + ADR 0018)
- Files touched (TDD only):
  - [.github/instructions/testing.instructions.md](.github/instructions/testing.instructions.md) — NEW
  - [.github/copilot-instructions.md](.github/copilot-instructions.md) — Phase 2.5 inserted
  - [package.json](package.json) — added `test`, `test:watch` scripts
  - [docs/adr/0018-tdd-as-first-class-skill.md](docs/adr/0018-tdd-as-first-class-skill.md) — NEW
- Files touched (memory infra, uncommitted, modified earlier in this conversation thread):
  - `C:\Users\Sory kaba\AppData\Roaming\npm\node_modules\@modelcontextprotocol\server-memory\dist\index.js` (out-of-tree)
  - [scripts/patch-mcp-memory.js](scripts/patch-mcp-memory.js) — NEW
  - [.github/instructions/persistence.instructions.md](.github/instructions/persistence.instructions.md) — Memory Keeper section rewritten
- Observable outcome: `npm test` runs all 9 test files (137/137 passing). MCP memory search returns sensible hits for multi-token queries. The agent now has a codified TDD discipline with rationalization-detection.

## The "Live" Context

- **Phase pipeline shape**: Phases 0 → 1 → 2 → **2.5 (NEW)** → 3 → 4. Phase 2.5 lives between System Integrity and Logic & UX Validation in [.github/copilot-instructions.md](.github/copilot-instructions.md).
- **TDD applyTo glob**: `src/lib/**/*.ts,src/app/api/**/*.ts,src/data/**/*.ts`. UI (`*.tsx`) deliberately uncovered.
- **Test scripts**: `npm test` = `vitest run`, `npm test:watch` = `vitest`.
- **9 existing test files** (the convention to follow): `src/lib/worklog-folders.test.ts`, `src/lib/worklog/prosemirror-to-text.test.ts`, `src/lib/worklog/parse-snapshot.test.ts`, `src/app/api/work-logs/[id]/route.test.ts`, `src/components/worklog/worklog-dnd-provider.test.ts`, `src/app/api/work-logs/reorder/route.test.ts`, `src/app/api/work-logs/mention-search/route.test.ts`, `src/app/api/work-logs/folders/reorder/route.test.ts`, `src/app/api/document-folders/[id]/route.test.ts`.
- **Workaround pattern for UI logic** (preserves "no UI test stack" boundary): extract pure helpers from `*.tsx` into a sibling `.ts` module and test that. See [src/components/worklog/worklog-dnd-provider.test.ts](src/components/worklog/worklog-dnd-provider.test.ts).
- **Recently-read files**: superpowers `using-superpowers/SKILL.md` and `test-driven-development/SKILL.md` (shape inspiration only — content is Resumsify-original).
- **Recently-rejected approaches**: 1B (all-TS scope — too wide), 1C (include `.tsx` — would block UI work, no DOM stack), 2A (forward-only retroactivity — too lenient, leaves bug-fix regressions ungated), 2B (strict superpowers stance — too rigid for current codebase), 3B/3C (TDD inside Phase 3 or Phase 1 — wrong placement).

## Next Immediate Step

> "Apply the TDD skill to the next logic-layer change. Before writing implementation, read [.github/instructions/testing.instructions.md](.github/instructions/testing.instructions.md), then RED → Verify RED → GREEN → Verify GREEN → REFACTOR. If the change is in `*.tsx`, the skill does not apply — proceed normally with the manual test sheet."

Pending also: **VS Code window reload** to refresh the MCP tool name registry to `mcp_memory-resumsify_*`, `mcp_memory-global_*`, `mcp_memory-user-profile_*` (naming patch is on disk + service is restarted, but the agent process holds a cached tool list).

## Unresolved Blockers

- [ ] **VS Code reload required** — MCP tool registry caches old names. After reload, verify the patched tool names appear before next memory write.
- [ ] **Memory infra commit** — the persistence.instructions.md rewrite + `scripts/patch-mcp-memory.js` are uncommitted (git status shows them as `M` and `??`). They are working on disk but should be committed before that work is at risk. Not committed in this session because the user only asked for a TDD checkpoint commit.
- [ ] **OneDrive worktree gc lock** — git commit prompts twice with `Deletion of directory '.git/worktrees/agents-{capabilities-overview,review-master-gallery-feature}/logs' failed. Should I try again? (y/n)`. Answer `n` to both — commit completes anyway. Documented in user memory under "Git Push Policy" but the worktree directories themselves should eventually be cleaned up out-of-band.
- [ ] **Many unrelated dirty files** — `git status` shows ~100+ deleted `.squad/**` files, several modified `*.tsx` files, two untracked ADRs (0016, 0017) and three untracked components. Not in scope for this session; user is deliberately holding them.

## Optional: Deferred Ideas

- **UI test stack (jsdom + RTL)** — parked in [/memories/repo/parked-ideas.md](/memories/repo/parked-ideas.md) with full re-entry criteria. Don't act without explicit re-confirmation.
- **Red Flags / Rationalization tables in `compliance.instructions.md`** — borrowing the superpowers tone-pattern beyond TDD. Discussed in the comparison response but not implemented; would harden the existing "Confidence Trap" rule.
- **"Verify Plan" checkpoint in Phase 1** — superpowers-inspired explicit acceptance assertion before writing code. Not a felt pain yet.

## UI Graph Status

UI Graph Keeper did not run — no UI code (`*.tsx`, className strings, Tailwind) was written or modified this session. All work was in instruction files, ADRs, package.json, and out-of-tree node_modules.

## Memory Graph Status

Memory Keeper audited the session — no new domain entities warranted. The TDD addition is an instruction-stack architecture fact (cross-workspace), not a domain fact, so it is recorded in `/memories/resumsify-lessons.md` rather than the MCP graph. No `mcp_memory_create_entities` / `add_observations` / `create_relations` calls were made.
