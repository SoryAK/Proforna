export const metadata = {
  title: "Recruiter Portal - Resumsify",
  description: "Submit job opportunities and connect with the candidate",
};

export default function PortalLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
